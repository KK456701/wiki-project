# 指标核算证据、数据链路与汇总口径验收记录

## 最终验收批次

- 验收时间：2026-08-02（Asia/Shanghai）
- 浏览器输入：`计算去年全部指标的结果`
- 批次：`BJOB_9d5aa9a8011b4ac2b911`
- Trace：`TRACE_465a136e6e06466a`
- 统计窗口：2025-01-01 至 2026-01-01
- 批次状态：`PARTIAL_SUCCESS`（25 个口径计算成功、17 个无样本、1 个未实现、0 个非预期执行失败）

## 验收结论

- “数据初始化校验”在计算开始后立即出现，初始显示 `0/68` 个去重检查对象，并明确标注 `35 个指标 / 43 个口径`。
- 完成后“本次指标核算”只保留 7 个业务阶段节点，不再用口径重复次数制造大量节点。
- 初始化校验覆盖 43 个口径；抽取为 `38/38` 个需抽取口径；真实库快照为 `41/41` 个快照一致。
- HXZD-002-001 显示为 `2 类问题 · 5 条原始证据`：4 个派生字段归入同一问题组，窗口探针问题独立保留。
- HXZD-002-001 数据链路明确区分主输入 `MRAS_BUSINESS_WARDROUND` 与参数侧输入 `MRAS_TARGET_DEFINITION`。
- 指标级汇总为：覆盖 35、达标 7、未达标 10、待确认 18、质量正常/异常 0/35。
- 数量恒等式成立：`7 + 10 + 18 = 35`，`0 + 35 = 35`。
- 切换到其他历史对话后再返回，能够按 `batchRunId → traceId` 懒加载恢复完整 7 节点证据链，没有重新计算。

## 证据文件

- `batch-snapshot.json`：最终批次和 43 个口径结果快照。
- `trace.json`：完整运行 Trace。
- `43-profile-matrix.csv`：43 个口径验收矩阵。
- `35-indicator-matrix.csv`：35 个指标级汇总矩阵。
- `01-initialization-immediate.png`：初始化节点即时出现。
- `02-summary-35-indicators.png`：35 指标汇总。
- `03-hxzd-002-001-grouped-evidence.png`：重复证据分组且原始证据完整。
- `04-data-flow-primary-parameter.png`：主输入与参数表侧输入。
- `05-history-trace-restored.png`：历史对话恢复证据链。
- `06-final-release-browser.png`：最终发布构建的浏览器结果。

## 自动化验证

- 前端类型检查：通过。
- 前端生产构建：通过。
- 后端完整测试：396 个测试通过，0 失败，0 错误。
- 本次相关后端定向测试：26 个测试通过。
- 单体 JAR 构建：通过。
- `/api/health`：`ok`。
