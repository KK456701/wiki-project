# 数据初始化校验验收记录（2026-08-01）

## 验收环境

- 页面：`http://127.0.0.1:8765/`
- 输入：`计算去年全部指标的结果`
- 模型：Qwen-Plus 7B（阿里云百炼 API）
- 知识库：`knowledge-index_backup_20260801_150233`
- Trace：`TRACE_00cd3d141d5f4f62`
- 批次：`BJOB_fdc6e214a16c4f0cadbb`
- 统计窗口：2025-01-01 至 2026-01-01

## 浏览器验收结论

- 初始化节点在请求发出约 3.5 秒后出现，并实时显示检查阶段。
- 最终执行区为 9 个关键节点，只有一个“数据初始化校验”节点。
- 43 个口径全部保留并完成状态闭合：25 个成功、17 个无样本、1 个未实现。
- 初始化结论：41 个可继续、1 个初始化阶段无样本、0 个被阻断、1 个未实现/跳过。
- `HXZD-009-002_002` 只显示一次“未实现/跳过”，没有重复产生缺源 SQL、缺目标表等错误。
- 三个真实库直接查询口径不再因为没有业务库源 SQL或中间目标表而被错误阻断。
- 真实库本次数据校验为 41/41 全部一致。
- 初始化详情按“影响等级 → 指标/口径 → 问题类型 → 字段或关联证据”折叠展示。
- “技术原始数据（实施排查用）”位于页面底部并默认折叠。

## 无法归属证据保留情况

解析器只消除了能够由查询块作用域和派生表输出明确证明的假歧义，没有删除真实的不确定项。浏览器中仍可见以下证据：

- `查询块 SELECT@0 / 派生表 T6 / 输出字段 T6.SHOULDNUM 无法追溯`
- `查询块 SELECT@0 / 派生表 T6 / 输出字段 T6.NORMALNUM 无法追溯`
- `查询块 SELECT@0 / 派生表 T2 / 输出字段 T2.RN 无法追溯`
- `查询块 SELECT@0 / 派生表 T5 / 输出字段 T5.RN 无法追溯`

这些记录均明确标注“已跳过该字段的空值率或关联覆盖检查，不代表数据库异常”。无法判断检查由改造前的 327 项降至 207 项，减少的是可确定修复的假歧义，不是隐藏问题。

## 自动化验收

- Maven 完整测试通过。
- Vue 类型检查通过。
- Vue 生产构建通过。
- 单体 JAR 构建通过并加载 43 个知识库实体。
- 本地 DBHub MCP（8080）和 Java 服务（8765）均保持运行。

## 证据文件

- `01-final-43-overview.png`：43 个口径闭合结果。
- `02-initialization-layered-detail.png`：分层详情与保留的派生查询证据。
- `trace-TRACE_00cd3d141d5f4f62.json`：170 条 Trace 节点元数据；初始化节点完整内容仍保存在运行库，文件记录其完整长度。
- `43-profile-matrix.csv`：43 个口径的最终状态、结果、分子、分母和错误码矩阵。
