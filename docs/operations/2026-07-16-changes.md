# 2026-07-16 对话变更总结

## 2026-07-17：下线旧稳定流程并增加 Qwen3 8B 思考模型

- 删除旧 `/api/chat`、`/api/chat/stream`、LangGraph 对话实现和 Shadow Runtime。
- 前端只保留登录态 Agent SSE，不再显示“稳定流程”或自动回退旧请求。
- capabilities 不再返回 legacy/shadow 模式，只返回 Agent 可用状态、模型列表和编排类型。
- 新增 `ollama-qwen3-8b-thinking`，实际模型为本机 `qwen3:8b`，Ollama 请求携带 `think: true`。
- 原始思考内容不进入公开 SSE、Trace 摘要和最终回答。
- 当前架构和生产 LLM 提示词清单见 `docs/architecture/agent-runtime-current.md`。

## 对话目标

1. 排查并修复 DeepSeek API 工具调用 Agent 的稳定性问题
2. 新增 Excel 指标明细上传与分析功能
3. 优化 Agent Runner 的异常处理与工具可用性逻辑

---

## 一、Bug 修复

### 1. `_rule_evidence` 默认 fact_types 缺少 `"rule_identity"`

- **文件**: `app/agent_tools/read_tools.py`
- **问题**: `get_effective_rule` 和 `inspect_indicator_implementation` 返回的 evidence 默认 fact_types 不含 `"rule_identity"`，导致 `has_verified_rule()` 返回 False，`prepare_indicator_sql` 等下游工具被标记为不可用
- **修复**: 在 `_rule_evidence` 默认 fact_types 中增加 `"rule_identity"`

### 2. `prepare_indicator_sql` 被旧 SQL ID 阻塞

- **文件**: `app/agent_tools/sql_tools.py`
- **问题**: 可用性检查 `_state_has_verified_rule` 中包含 `and not has_active_sql(state)`，上一轮遗留的 SQL ID 导致新一轮 prepare 被封死
- **修复**: 移除该条件，允许在规则已验证时随时 prepare 新 SQL

### 3. `search_indicator_rules` 重复调用

- **文件**: `app/agent_tools/read_tools.py`, `app/agent_runtime/contracts.py`
- **问题**: 模型在已确认指标后仍重复搜索，浪费 token 和步骤
- **修复**: 在 `AgentRunState` 中加入 `search_count` 计数器，`search_indicator_rules` 每轮最多调用 1 次（`availability=lambda ctx, state: state.search_count < 1`）

### 4. Response Guard 无限循环

- **文件**: `app/agent_runtime/runner.py`
- **问题**: 中文检查、trial_run 检查、missing_fact_types 检查无纠正次数上限，模型落入死循环直奔 max_steps
- **修复**: 四个 guard 全部加 1 次纠正上限（`chinese_corrections`、`trial_run_corrections`、`fact_corrections`、`evidence_corrections`）

### 5. 异常被静默吞掉

- **文件**: `app/agent_runtime/runner.py`, `app/agent_runtime/service.py`
- **问题**: 多处 `except` 块无日志，异常无法定位
- **修复**: 所有异常路径加 `print(traceback)` 和 `[AGENT-XXX]` 标识日志；工具执行代码、模型调用、流式任务均包裹 try/except

### 6. 模型输出对话格式前缀

- **文件**: `app/agent_runtime/response_guard.py`, `app/agent_runtime/prompts.py`
- **问题**: DeepSeek 模仿历史消息格式输出 "用户:"、"助手:" 前缀
- **修复**: `normalize_agent_answer` 自动剥离前缀；提示词禁止对话格式输出

### 7. 模型选择器在发送时被重置

- **文件**: `web/agent-runtime.js`, `web/index.html`
- **问题**: 每次发送前 `refreshAgentRuntimeMode()` 用服务端默认模型重建下拉框，覆盖用户手动选择
- **修复**: `renderModelSelector` 保留用户已选值；移除 `streamChat` 中的冗余刷新调用

---

## 二、系统提示词优化

- **文件**: `app/agent_runtime/prompts.py`

| 规则 | 效果 |
|---|---|
| `prepare_indicator_sql` 支持任意时间范围 | 模型不再声称"无法为新时间段生成SQL" |
| 假设性问题 vs 正式变更请求的区分 | 用户问"如果按X算"不会再误触发审批流程 |
| 解释性问题不引用具体数值 | "分子分母怎么来的"不会触发 trial_run guard |
| "文件编号"识别规则 | 用户上传文件后能用自然语言触发分析 |
| 禁止重复搜索 | 已确认指标后禁止 search_indicator_rules |

---

## 三、新增功能: Excel 指标明细上传与分析

### 后端

| 文件 | 变更 |
|---|---|
| `app/agent_tools/upload_tools.py` | **新建**。`analyze_uploaded_indicators` 工具：解析 Excel 表头/行数/数值列统计，自动检测指标结构（分子/分母/指标率列），与当前系统试运行结果对比 |
| `app/agent_tools/catalog.py` | 注册 `UploadToolServices` + `build_upload_tools` |
| `app/agent_tools/__init__.py` | 导出 Upload 相关类 |
| `app/api/agent_routes.py` | 新增 `POST /api/agent/upload` 端点（.xlsx/.xls，最大 10MB，按 hospital_id 隔离） |
| `app/agent_runtime/service.py` | `_build_runner` 注入 `UploadToolServices` |

### 前端

| 文件 | 变更 |
|---|---|
| `web/index.html` | 输入框旁加 📎 上传按钮；上传后自动显示确认消息（含 file_key）；预填自然语言分析请求 |

### 使用方式

1. 点击 📎 上传 Excel
2. 聊天区显示 `已上传: xxx.xlsx + 文件编号: hospital_001_xxx`
3. 输入框自动填"帮我分析一下刚上传的指标文件"，用户可自由修改
4. 模型调用 `analyze_uploaded_indicators` 解析、统计、对比

---

## 四、涉及文件清单

| 文件 | 操作 |
|---|---|
| `app/agent_runtime/contracts.py` | 改：加 `search_count` |
| `app/agent_runtime/prompts.py` | 改：多项提示词优化 |
| `app/agent_runtime/runner.py` | 改：异常处理、guard 上限、日志 |
| `app/agent_runtime/response_guard.py` | 改：剥离对话前缀 |
| `app/agent_runtime/service.py` | 改：上传服务注入、日志 |
| `app/agent_tools/read_tools.py` | 改：fact_types + search 限制 |
| `app/agent_tools/sql_tools.py` | 改：移除 has_active_sql 阻塞 |
| `app/agent_tools/catalog.py` | 改：注册上传工具 |
| `app/agent_tools/__init__.py` | 改：导出 Upload 类 |
| `app/agent_tools/upload_tools.py` | **新建** |
| `app/api/agent_routes.py` | 改：上传端点 |
| `web/index.html` | 改：上传按钮 + JS 逻辑 |
| `web/agent-runtime.js` | 改：模型选择器修复 |

---

## 五、Planner / Compiler / Controller / Verifier 编排

为提升 4B 本地模型的多步工具调用稳定性，工具调用型 Agent 新增服务端计划编译与受控执行层：

- Planner 只输出业务目标、目标指标、原始时间表达、请求输出、约束和歧义；严格拒绝 `steps`、`proposed_steps`、`tool` 等字段。
- PlanCompiler 根据业务能力依赖生成正式执行计划，不让模型维护具体工具名和调用顺序。
- StateController 根据当前 Evidence 确定下一项业务能力；确定性参数编译器把该能力映射为唯一工具和完整参数。
- TimeRangeResolver 确定性解析本月、上月、今年至今、从某月到现在和明确日期范围。
- PlanVerifier 校验必要事实、分子/分母/指标值、SQL ID、医院、数据源、统计周期与 `context_digest`。
- ReplanPolicy 只允许计划方向错误重规划一次；参数缺失由控制器补步骤或向用户澄清。
- 人工/终止分类包括用户澄清、业务确认、管理员审批、实施支持、系统运维和安全拒绝。

正式 Agent 服务默认启用 `plan_compile_control`；旧聊天、Shadow 分流及其专用意图识别/答案生成提示词已经删除，不再作为回退路径。

---

## 六、2026-07-17 删除旧聊天提示词

- 删除 `legacy_chat_intent.txt`、`legacy_chat_answer.txt` 及专用加载函数。
- `HumanInteractionAgent` 只保留业务 API 复用的确定性规则处理，不再接受 LLM 客户端。
- 提示词目录只登记当前仍在使用的 Planner、Executor、Replanner、指标草稿和诊断提示词。
- 完整回归结果：787 passed，1 skipped。

---

## 七、2026-07-17 确定性工具直调

- 新增 `app/agent_planning/dispatch.py`，根据已编译计划、结构化状态和已验证 Evidence 确定性生成工具调用参数。
- 计划内的指标搜索、生效口径读取、实施检查、SQL 准备、只读试运行、诊断、规则变更预览和上传分析不再调用 Executor 选择工具。
- 当前对话主链改为 `Planner → Compiler/Validator → StateController → DeterministicDispatch → ToolGateway → Verifier → FinalAnswerLLM`。
- Qwen3 8B 的 `think: true` 只用于最终回答；Planner 继续关闭思考，工具步骤不再产生数十秒的模型路由耗时。
- 最终回答模型返回空内容且没有工具调用时记录 `MODEL_EMPTY_ACTION` warning，并使用集中纠错提示最多重试一次。
- Trace 新增“编译工具调用 / `deterministic_tool_dispatch`”代码节点；Ollama 只记录是否存在思考、思考字符数和结束原因，不保存原始隐藏思维。
- `agent_executor.txt` 与 `agent_executor_step.txt` 已收敛为最终回答提示词，文件角色和触发时机同步登记在 `app/prompts/README.md`。
- 自动化验证：Ruff、`compileall` 和完整测试通过，结果为 793 passed、1 skipped。
