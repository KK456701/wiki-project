# 核心制度指标 Agent 项目交接文档

> 更新时间：2026-07-26
>
> 当前有效运行时：Java 单运行时
>
> 当前分支：`main`
>
> 本次交接生成基线：`37093cacb9ec42aa6587cac00a50bbb5b76e25dc`
>
> 基线提交：`docs: 更新Java Agent项目交接文档`

本文是新对话继续开发时的唯一当前交接入口。旧日期交接文档、旧 Python 实现、
MQSI 示例数据和历史架构说明只用于追溯，不能作为当前运行依据。

## 1. 当前仓库状态

### 1.1 技术栈

| 层级 | 当前实现 |
|---|---|
| 后端 | Java 17、Spring Boot 3.5.16、Spring AI 1.1.8 |
| 前端 | Vue 3.5.22、TypeScript 5.9.2、Vite 7.1.7 |
| 运行数据 | SQLite |
| 指标知识 | HXZD 版本化文件 Wiki |
| 医院数据访问 | DBHub sidecar，只读 |
| 本地模型 | Ollama Qwen3 4B、Qwen3 8B 思考模式 |
| API 模型 | DeepSeek、阿里云百炼 Qwen3 14B |
| 部署形态 | Spring Boot 单 JAR，内置 Vue 静态资源 |

当前生产运行不依赖 Python 或 MySQL，不使用 LangChain、LangGraph、外部任务队列、
Prometheus、Grafana 或新的中间件。

### 1.2 当前知识版本

| 类型 | Release | 准确含义 |
|---|---|---|
| 公司指标知识 | `KB-20260726-7e71f92e431a` | 35项指标定义、Profile和SQL的基础知识版本 |
| `hospital_001` 双库执行契约覆盖层 | `KB-20260726-HOSPITAL001-DUALDB` | 在公司知识之上补充数据库角色、结果列和验证状态，不是医院独立口径 |

必须特别注意：

- `KB-20260726-HOSPITAL001-DUALDB` 的原始资料及 SHA-256 与公司知识相同；
- 它依赖的基础版本是 `KB-20260726-7e71f92e431a`；
- 它的 `hospital_overrides` 为空；
- 它由提交 `68878434 fix: 发布医院双库执行契约` 创建；
- 当前仍放在 `releases/hospitals/hospital_001/` 下只是为了保持运行时指针兼容。

因此，后续代码和文档必须称它为“医院范围的执行契约覆盖层”，不能将其作为本院
业务口径的证据。只有未来由医院原始差异资料或医院回收包生成、经过人工审核并且
`hospital_overrides` 非空的 Release，才可以称为“医院差异知识”。

当前知识契约包含：

- 35 项核心制度指标；
- 45 个独立 Profile；
- 169 个 SQL 块；
- `etl_source`、`overview`、`department_detail`、`patient_detail`
  四类 SQL 能力引用；
- 版本、医院范围、参数、结果列、比较键和执行门禁。

35 项指标都支持检索和规则解释。双库执行契约覆盖层为 35 项指标提供受控概览 SQL；
患者明细、科室明细、差异导出只对已完成对应比较键和字段契约的 Profile 开放。
草稿或只有文档的 Profile 不允许模型临时生成 SQL 绕过门禁。

### 1.3 本次交接验证结果

| 验证项 | 结果 |
|---|---|
| Java 全量测试 | 226 passed，0 failed，0 skipped |
| Vue 生产构建 | 通过 |
| Wiki 机器契约 | 35 指标、45 Profile、169 SQL 引用，校验通过 |
| Wiki Markdown 干跑 | 35/45/169，校验通过 |
| 知识发版脚本测试 | 4 passed |
| `git diff --check` | 通过 |

交接文档所在提交可在拉取后执行 `git log -1 --oneline` 查看。文档不能安全地硬编码
它自身尚未生成的 Git 哈希，因此这里记录的是已经验证过的功能代码基线。

## 2. 当前 Agent 架构

```mermaid
flowchart LR
    U["用户问题 / 上传文件"] --> C["读取结构化会话上下文"]
    C --> R["指标识别<br/>规则 + 本地语义 + 允许列表 LLM 消歧"]
    R --> S{"一个还是多个指标"}
    S -->|多个| CS["服务端确定性拆分子任务"]
    S -->|一个| P["Planner<br/>生成 RequestPlan v3"]
    CS --> P
    P --> A["目标—计划一致性校验"]
    A -->|允许的语义方向错误| RP["Replanner<br/>最多一次"]
    RP --> A2["重新校验"]
    A -->|一致| IR["Compiled Plan IR"]
    A2 --> IR
    IR --> SC["确定性状态控制器"]
    SC --> D["Capability Registry / Dispatch"]
    D --> G["ToolGateway<br/>权限、参数、超时、重复调用和安全策略"]
    G --> T["Wiki / SQL / DBHub / 上传 / 诊断工具"]
    T --> E["Evidence 记录与验证"]
    E --> V{"已满足所需事实"}
    V -->|否| SC
    V -->|是| F["Final Answer<br/>按需回答模板"]
    F --> M["保存会话和 Trace"]
```

关键原则：

1. Planner 只理解目标，不输出工具名、不写 SQL、不决定执行步骤。
2. 工具顺序来自 Java 的 `CapabilitySpecRegistry` 和 Compiled Plan IR。
3. 状态控制器每轮只推进一个尚未完成的事实，便于验证、短路、重试、审计和防止
   4B/8B 模型自由循环。
4. Replanner 只处理意图方向或任务类型错误，不处理数据库、权限、缺时间、Evidence
   冲突和普通工具错误。
5. Final Answer 只消费已验证 Evidence，不访问数据库，也不从历史回答回忆数值。

## 3. 指标和意图识别

### 3.1 指标识别

`HybridIndicatorResolver` 的顺序是：

```text
规则编号/标准名称精确匹配
→ Wiki 别名与关键词
→ 本地字符与语义评分
→ 候选仍接近时，让 LLM 只能从候选 rule_id 中选择
```

HXZD Wiki 是指标身份的权威来源。术语库只能补充别名，旧 MQSI 链接不能重新成为
运行候选。精确匹配不调用 LLM，小模型最多接收 1～3 个紧凑候选卡。

一段话包含多个指标时，服务端先从 Wiki 命中集合和语句边界确定性拆分子任务；
每个子任务使用独立 `subtask_id`、状态、Evidence 和 Trace 泳道，最终按用户输入顺序
合并回答。

### 3.2 RequestPlan v3

主要字段：

- `intent`：用户要解释、计算、SQL、候选口径、上传分析还是差异诊断；
- `target_indicator`：指标原文和确认后的 `rule_id`；
- `target_caliber`：候选口径原文和 `profile_id`；
- `time_expression`：时间原文及解析后的左闭右开边界；
- `requested_outputs`：本轮必须产出的业务事实；
- `explanation_focuses`：规则解释实际关注的部分；
- `constraints`、`semantic_ambiguities`、`confidence`。

`explanation_focuses` 支持：

```text
overview
definition
formula
numerator
denominator
time_dimension
deduplication
exclusions
version_scope
```

“分子是什么”“分母怎么算”“按哪个时间字段统计”等明确追问由 Java 复用当前会话
中的指标，跳过 Planner，只加载对应分项模板。“分子和分母怎么来”只组合这两个部分，
不再重复完整指标摘要、版本、公式和实施状态。复杂问法才由 Planner 输出一个或多个
关注点。历史 v2 计划缺少该字段时按 `overview` 兼容。

## 4. 已实现的业务能力

### 4.1 规则与 SQL

- 指标定义、公式、分子、分母、统计时间、去重、排除、版本范围的完整或分项解释；
- 当前口径和候选口径列表查询；
- 已审批候选口径的受控模拟；
- 展示知识库概览 SQL；
- SQL 参数、字段映射、只读安全和结果列契约校验；
- 不允许用户或模型提交任意 SQL、物理字段或参数覆盖。

### 4.2 计算与双库核对

当前只允许两个数据库角色：

| 角色 | DBHub source |
|---|---|
| 业务库 | `winex_all_dev` |
| 真实库 | `winex_aima` |

旧数据库已退役。数字 `991827` 只作为受控医院范围参数
`hospital_scope_value`，不是数据库编号。

普通指标计算也固定执行双库概览核对：

```text
校验统计区间
→ 准备同一 Profile 的 overview SQL
→ 业务库执行
→ 真实库执行
→ 服务端复算并比较分子、分母
```

- 查询区间统一为左闭右开；
- 单个子任务最多查询 `start.plusMonths(1)`，超出时在 DBHub 前阻断；
- 两边分子、分母都相同才算一致；
- 比例相同但分子或分母不同仍判定不一致；
- 两库严格串行，避免给数据库增加并发压力；
- 任一侧缺表、缺字段、函数或结果列时返回结构不兼容，不伪装成业务差异。

双库概览不一致时，若 Profile 具备完整明细契约，才继续在两个库执行
`department_detail` 和 `patient_detail`，按科室键和业务主键形成交集、单边记录、
字段差异和分子判定差异。契约不完整时保留“概览不一致”事实，但不猜测原因。

### 4.3 差异诊断

“我们 100、系统 98”“上传文件为什么和系统不同”等请求进入固定分层诊断：

1. 范围与文件预检；
2. 实时结构核验；
3. 当前口径与已审批候选口径反事实试算；
4. 记录集合与数据质量核对；
5. 生成有证据限制的诊断结论。

候选口径结果偶然相同不能直接确认原因，必须同时能解释受影响记录。汇总文件没有
逐条业务键时，只能确认汇总差异，不能编造“仅系统有/仅文件有”的具体记录。

### 4.4 上传、明细和导出

- Excel 上传只需要 `multipart/form-data` 的 `file` 字段；
- 文件名和文件大小由浏览器与服务端自动获得，不是额外业务入参；
- 支持汇总表解析、逐条明细比较和安全差异导出；
- 患者级原始行不进入通用 Trace 或 Evidence；
- 明细对象按医院、用户、时效和权限隔离。

### 4.5 回答模板与追问

Final Answer 按 `intent + requested_outputs + explanation_focuses` 从
`backend-java/src/main/resources/answer-templates/` 只加载本轮需要的模板。
模板正文不塞入全局 Prompt。

模型若遗漏必需章节、泄漏工具协议、加入未要求的口径章节或引用未验证数值，回答契约
会拒绝并纠错；再次失败时使用相同粒度的确定性 Evidence 模板。指标、统计时间或候选
口径不唯一时，服务端返回可点击澄清选项，而不是直接结束对话。

### 4.6 Trace 架构图

“查看链路”默认且只展示六阶段横向架构图：

1. 上下文与指标识别；
2. 规划与目标校验；
3. IR 编译与能力选择；
4. 工具与数据库执行；
5. Evidence 验证与安全检查；
6. 回答组织与会话保存。

LLM、Java、工具/数据库、存储和失败节点使用不同颜色；多指标按泳道展示。点击节点可
查看完整的安全输入、输出、配置、耗时、模型和版本。Planner 节点同时展示模型原始
输出、规范化计划和字段中文解释。确定性分项追问会明确显示“未调用 LLM Planner”
及 `explanation_focuses`。

## 5. 模型配置

当前模型注册项：

- `ollama-qwen3`：本地 Qwen3 4B；
- `ollama-qwen3-8b-thinking`：本地 Qwen3 8B 思考模式；
- `aliyun-qwen3-14b`：阿里云百炼 Qwen3 14B；
- `deepseek-v4-flash`；
- `deepseek-v4-pro`。

模型只参与 Planner、允许列表内消歧、少数目标一致性审核、Replanner 和 Final Answer。
日期边界、IR、工具选择、SQL、权限、双库比较、Evidence 验证和 Workflow 不由模型
自由决定。API Key 仅通过环境变量提供，不得写入配置示例、日志、知识库或 Git。

## 6. 知识库更新

版本化知识库结构：

```text
core-rules-wiki/
├── raw/
├── review/
├── releases/
├── pointers/
├── prompts/
└── schema/
```

更新流程：

```text
Excel / Markdown
→ 确定性提取
→ 大模型结构化规范化
→ review/pending
→ 静态、参数、函数、元数据、编译和试运行门禁
→ 人工确认发布
→ 原子切换 current 指针
```

大模型只能整理定义、公式、Profile 和来源关系，不能重写或补造 SQL。SQL 原文按
`sql_block_id` 引用；`#NAME?`、未知函数、未知参数、未解析模板、多语句或写操作会
阻止执行发布。发布失败不影响当前版本，回滚只切换 pointer。

常用校验：

```powershell
node .\scripts\build-wiki-from-markdown.mjs `
  --input ".\core-rules-wiki\raw\company\35项核心制度指标完整提取.md" `
  --check

node .\scripts\validate-wiki-contract.mjs `
  --expected-indicators 35 `
  --expected-profiles 45 `
  --expected-sql 169

node --test .\scripts\tests\knowledge-release.test.mjs
```

## 7. 待合并：同事的源数据抽取接口

### 7.1 当前状态

以下内部契约已经存在：

- `SourceExtractionGateway`
- `ExtractionRequest`
- `ExtractionResult`
- `UnavailableSourceExtractionGateway`

当前默认：

```text
AGENT_EXTRACTION_MODE=disabled
```

该模式明确记录 `SKIPPED`，不会伪造一次成功抽取，但允许继续执行业务库和真实库的
只读概览核对。真实抽取接口尚未合入，所以不得切换为 `required`。

### 7.2 同事代码合并方式

拿到同事代码后，新增一个 `SourceExtractionGateway` 适配器并注册为 Spring Bean。
不要把同事的 HTTP 字段、鉴权协议或临时返回格式侵入
`DualDatabaseIndicatorExecutionWorkflow`。

适配器必须传递或映射：

- `traceId`、`subtaskId`；
- `hospitalId`、`userId`；
- `releaseId + ruleId + profileId`；
- `statStart`、`statEnd`；
- 当前发布 Wiki 的 `etl_source` SQL；
- SQL SHA-256；
- 受控参数；
- 业务库和真实库 source ID；
- 本轮幂等键。

返回值必须映射：

- extraction ID；
- 成功、失败状态；
- 抽取、插入、更新、拒绝数量；
- 完成时间；
- 源与目标快照引用；
- 稳定错误码和安全说明。

强制约束：

1. 每个指标子任务每轮只抽取一次，后续概览和明细诊断复用该结果。
2. 同一轮重试复用幂等键，新一轮重新生成。
3. SQL 只能来自当前发布且已验证的 Wiki，不能来自用户或模型。
4. 抽取失败必须阻断双库计算，不能降级成“假装已抽取”。
5. Java 和 DBHub 保持只读，真实库写入只由外部抽取接口完成。
6. 认证、地址和密钥只使用环境变量，日志不得记录凭据、患者行或完整敏感响应。

### 7.3 切换 `required` 前的验收

- 完成接口协议和错误码联调；
- VPN 和 DBHub 均可用；
- 使用不超过一个月的真实指标范围抽样；
- 验证一次成功抽取后固定顺序为：业务库概览 → 真实库概览；
- 验证相同幂等键重试不会重复写入；
- 验证抽取超时、拒绝、部分失败都会阻断双库计算；
- 验证一轮进入科室/患者诊断时不会再次抽取；
- 验证进程内信号量能串行同医院抽取；
- 完成回退演练后再设置 `AGENT_EXTRACTION_MODE=required`。

## 8. 构建、启动与测试

### 8.1 配置

```powershell
Copy-Item .\config.example.yaml .\config.yaml
```

必须确认：

- 运行数据库使用 SQLite；
- DBHub 只有 `winex_all_dev` 和 `winex_aima` 两个角色；
- 本地或 API 模型至少一个可用；
- 不提交真实密码、令牌、连接串、运行数据库和日志；
- 抽取适配器合入前保持 `AGENT_EXTRACTION_MODE=disabled`。

### 8.2 构建单 JAR

```powershell
powershell -NoProfile -ExecutionPolicy Bypass `
  -File .\scripts\build-java-vue.ps1
```

输出：

```text
backend-java/target/wiki-agent-java-0.1.0-SNAPSHOT.jar
```

### 8.3 启动

```powershell
powershell -NoProfile -ExecutionPolicy Bypass `
  -File .\scripts\start-java-runtime.ps1 -Port 8765
```

访问：

- 页面：`http://127.0.0.1:8765/`
- 健康：`GET /api/health`
- 运行时：`GET /api/runtime/status`

DBHub 默认地址见 `backend-java/src/main/resources/application.yml`。真实业务库测试通常
还需要公司 VPN；VPN 或 DBHub 不可用属于外部依赖故障，不应触发 Replanner。

### 8.4 开发测试

```powershell
cd .\backend-java
mvn.cmd -s .\maven-settings.xml test

cd ..\frontend-vue
npm.cmd run build
```

## 9. 当前已知限制

1. 同事的外部抽取接口尚未合入，当前只是双库只读核对。
2. DBHub 和医院数据库真实验收依赖公司 VPN。
3. 部分 Profile 只有概览能力，没有已验证的科室/患者比较契约。
4. 仅有汇总 Excel 时不能定位逐条差异原因。
5. 本地 4B/8B 在复杂语义上弱于 API 模型，因此关键安全路径由 Java 确定性控制。
6. 监控页面、指标草稿/发布工作台和“全面实施验收”已经永久移除，不要恢复。
7. 历史 SQLite 数据可以审计查看，但旧数据库运行对象不得重新执行。

## 10. 新对话建议阅读顺序

1. 本文；
2. `README.md`；
3. `docs/architecture/agent-runtime-current.md`；
4. `docs/api/agent-run-trace-detail.md`；
5. `backend-java/src/main/resources/prompts/README.md`；
6. `backend-java/src/main/resources/answer-templates/README.md`；
7. `core-rules-wiki/pointers/company-current.json`；
8. `core-rules-wiki/pointers/hospitals/hospital_001-current.json`。

接手后的第一优先级是：等待并审查同事的抽取代码，按第 7 节实现 Gateway 适配器，
完成真实环境幂等与失败阻断验收；不要在接口尚未确认时猜测协议或提前切换
`required`。
