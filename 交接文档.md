# 核心制度指标 Agent 项目交接文档

> 更新日期：2026-07-28（以系统提交日期为准）
>
> 当前分支：`main`　最新基线提交：`7dec57b1`
>
> 本文是新对话/新同事继续开发时的唯一当前交接入口。旧日期交接文档、旧 Python 实现、
> core-rules-wiki 运行时链路和历史架构说明只用于追溯，不能作为当前运行依据。

---

## 1. 项目概述

面向医院本地化部署的医疗质量管理 AI Agent 系统，支持用自然语言查询、计算、诊断医院
核心质量指标（如"患者入院48小时内转科的比例"，rule_id `HXZD-001-001`）。

架构遵循确定性 8 阶段 Agent 流水线：

```
指标解析 → Planner → 目标对齐校验 → IR 编译/校验 → 状态控制+确定性分发
→ ToolGateway → 证据账本+校验器 → 最终回答合成
```

关键原则（长期有效，勿破坏）：

1. Planner 只理解目标，不输出工具名、不写 SQL、不决定执行步骤；
2. 工具顺序来自 Java 的 `CapabilitySpecRegistry` 和 Compiled Plan IR；
3. 状态控制器每轮只推进一个尚未完成的事实，防止 4B/8B 小模型自由循环；
4. Final Answer 只消费已验证 Evidence，不访问数据库，不从历史回答回忆数值；
5. 日期边界、IR、工具选择、SQL、权限、Evidence 验证均由 Java 确定性控制，不由模型决定；
6. API Key 只走环境变量，不写入配置示例、日志、知识库或 Git。

## 2. 技术栈

| 层级 | 当前实现 |
|---|---|
| 后端 | Java 17、Spring Boot 3.5.16、Spring AI 1.1.8（约 188 个 Java 文件） |
| 前端 | Vue 3 + TypeScript + Pinia + Vite（约 17 个 Vue/TS 文件） |
| 运行数据 | SQLite（`F:\A-wiki-project\runtime\wiki_agent_runtime.db`，会话/Trace/运行状态） |
| 指标知识 | 领导知识库 knowledge-index-mras（MRAS 实体页 + runtime.json 四层 SQL） |
| 医院数据访问 | DBHub sidecar（MCP 协议）；抽取写入由 biz-mcp 接口完成 |
| 模型 | Ollama（qwen3-4b / qwen3-8b-thinking）、DeepSeek（v4-flash / v4-pro）、阿里百炼（qwen3-14b） |
| 部署形态 | Spring Boot 单 JAR，内置 Vue 静态资源 |

前后端约定：后端 JSON 一律 snake_case，前端 store 里转 camelCase。

## 3. 代码结构

```
f:\A-wiki-project\
├── backend-java\            后端（注意：内含独立 .git，指向公司 TFS，绝对不要 push 它！见 §9）
│   └── src\main\java\com\hospital\wikiagent\
│       ├── agent\batch\     批量指标计算：BatchIndicatorRuntime（串行 concurrency=1）、
│       │                    IndicatorExecutionResult（24 字段 record，dataFreshness 为第 19 个）、
│       │                    BatchResultAggregator（确定性 Markdown 报告，禁止模型改写数值）
│       ├── agent\mras\      MrasSqlExecutionService（抽取+四层 SQL 执行）、MrasTemplateRenderer
│       │                    （#ETC{}/#EQUALS{} 模板渲染+方言修正）、EntityPageParser（启动扫描
│       │                    27 个实体页并缓存）、MrasParameterMapper
│       ├── agent\planning\  Planner、意图路由（含确定性意图升级 SQL_PREPARE→TRIAL_RUN）
│       ├── dbhub\           DbHubMcpClient / DbHubProperties
│       ├── auth\            HospitalAuthService（无效/缺失 token 自动降级 guest）
│       └── sync\            SyncDataService（同事代码：源库→中间表，DELETE+批量INSERT，
│                            Snowflake 主键，只插目标表存在的列）
├── frontend-vue\src\
│   ├── api\agent.ts         SSE 事件类型（AgentEvent，含 data_freshness）
│   ├── stores\agent.ts      会话与批量结果 store（BatchIndicatorResult）
│   ├── components\IndicatorResultCards.vue   指标结果卡片（口径/核算方式/明细三页签+旧数据警告条）
│   └── styles\main.css
├── scripts\                 dev-run.ps1 / build-java-vue.ps1 / start-java-runtime.ps1
└── runtime\                 运行时数据（SQLite、日志），已 gitignore
```

## 4. 环境与配置

- **后端端口固定 8765**；页面 `http://127.0.0.1:8765/`；健康检查 `GET /api/health`。
- **抽取地址（重点，勿改回）**：`application.yml` 中
  `dbhub.biz-mcp-url: ${WIKI_DBHUB_BIZ_MCP_URL:http://172.16.9.73/mras-mcp-mdm/http/mcp}`
  占位符是项目专属变量 `WIKI_DBHUB_BIZ_MCP_URL`；通用的 `DBHUB_BIZ_MCP_URL` 后端已不再读取
  （历史上曾三次被终端残留脏值静默覆盖导致抽取失败，详见 §8.1）。
- `dbhub.timeout-seconds: 120`（yml 覆盖代码默认 10 秒）。
- 后端日志落盘：`backend-java\runtime\logs\wiki-agent.log`（排查抽取链路先看这里）。
- SSE 超时 15 分钟（批量指标耗时长）；单次查询时间范围上限一年。
- 真实业务库联调通常需要公司 VPN；VPN/DBHub 不可用属于外部依赖故障，不应触发 Replanner。

## 5. 构建 / 启动 / 验证

### 5.1 日常开发（优先用这套）

```powershell
pwsh scripts/dev-run.ps1              # 免打包启动后端（mvn spring-boot:run，端口 8765）
pwsh scripts/dev-run.ps1 -Recompile   # 改代码后另开终端执行；devtools 约 10 秒自动热重启
```

注意：**devtools 热重启不刷新 JVM 环境变量**；改环境变量必须彻底杀进程重启，改 yml/class
只需 `-Recompile`。dev-run.ps1 启动时会对 `WIKI_DBHUB_BIZ_MCP_URL` / 残留 `DBHUB_BIZ_MCP_URL`
显式提示。

### 5.2 前端与测试

```powershell
cd frontend-vue; npm run build        # 含 vue-tsc 类型检查
cd backend-java; mvn.cmd -s .\maven-settings.xml test
```

### 5.3 正式打包

```powershell
pwsh scripts/build-java-vue.ps1       # Vue 构建 + mvn test & package → target/wiki-agent-java-0.1.0-SNAPSHOT.jar
pwsh scripts/start-java-runtime.ps1 -Port 8765   # 读 config.yaml、校验 Java 17 与端口后启动 jar
```

### 5.4 接口回放验证（无需登录）

无效/缺失 token 会被降级为 guest，可直接：

```
POST http://127.0.0.1:8765/api/agent/chat/stream
Authorization: Bearer guest
Content-Type: application/json
{"query":"计算去年患者入院48小时内转科的比例的结果"}
```

期望：SSE 依次返回 `batch_indicator_result`（如 10/417、SUCCESS，逐指标推送）→
`assistant_message`（Markdown 报告）→ `agent_done`；正常路径事件中**没有** `data_freshness` 字段。

## 6. 核心链路

### 6.1 批量指标计算与数据抽取（当前生产路径）

```
BatchIndicatorRuntime（Profile 批次从入口就串行，concurrency=1）
  → executeViaMras
    → MrasSqlExecutionService.executeOverview / executeDeptStat / executePatientDetail
      → ensureExtracted：查询前自动抽取（ExtractionCache 去重 TTL=30min）
        → SyncDataService.syncEventData：经 DbHubMcpClient JSON-RPC 调 biz-mcp，
          中间表先 DELETE 后批量 INSERT（Snowflake 主键，过滤目标表不存在的列）
      → executeSql：对中间表执行 MRAS 四层 SQL（ETL/overview/department/patient_detail）
```

- 抽取已默认开启（`extraction=required`，McpSyncSourceExtractionGateway 激活）；
  旧交接文档里"待合并的同事抽取接口 / AGENT_EXTRACTION_MODE=disabled"已成历史。
- 计算路径为**抽取 → 真实库概览**，不再执行旧的双库（业务库+真实库）核对。
- 单指标会展开为所有已审批 Profile 串行执行；分子/分母患者明细走 DetailResultCache（TTL=30min）。
- 真实库是全局单快照：每个 Profile 清理并替换同一组受控表后持锁计算，**并发会造成假性
  TOOL_TIMEOUT，串行设计勿改**。

### 6.2 抽取失败不再静默（7dec57b1 引入）

`ensureExtracted` 失败时返回原因（不阻断查询）→ 结果数据带 `extraction_warning` →
`IndicatorExecutionResult.dataFreshness = "extraction_failed_stale"` → SSE 事件带
`data_freshness` → Markdown 报告追加"⚠️ 数据抽取失败…基于中间表旧数据"脚注 →
前端卡片显示琥珀色警告条。另有既有约定值 `existing_snapshot_not_refreshed`（未刷新快照）。

### 6.3 指标与意图识别

`HybridIndicatorResolver`：规则编号/标准名精确匹配 → Wiki 别名关键词 → 本地语义评分 →
候选接近时 LLM 只能从候选 rule_id 中选。精确匹配不调用 LLM。多指标语句由服务端确定性
拆分子任务（独立 subtask_id/Evidence/Trace 泳道）。分子/分母/时间字段等明确追问由 Java
复用会话指标、跳过 Planner。存在确定性意图升级：带时间的"算结果"类问法从
INDICATOR_SQL_PREPARE 升级为 INDICATOR_TRIAL_RUN。

### 6.4 知识库（当前：knowledge-index-mras）

- 运行时知识已**完全切换**到领导知识库 knowledge-index-mras（775f4eb0），core-rules-wiki
  的运行时依赖已移除（其目录仍在仓库中仅作追溯）。
- MRAS 架构：每个指标一个实体页（共 27 个，启动时由 EntityPageParser 扫描缓存），
  四层 SQL（etl_source / overview / department / patient_detail）由 runtime.json 声明参数与
  能力契约；SQL 模板用 `#ETC{}`、`#EQUALS{}` 语法，由 MrasTemplateRenderer 渲染。
- EntityPageParser 只取源表第一个 SQL 块（避免 syncType 模板参数污染，e54eec08）。
- 医院侧差异只算"执行契约覆盖层"，除非未来出现 `hospital_overrides` 非空且人工审核的
  Release，否则不能称为"医院差异知识"。

### 6.5 前端交互（近期改版）

- 欢迎页为引导式面板（算指标/排查故障），指标多选 + 时间下拉（今年至今/最近一月/半年/
  3月/1月/本月/上月/自定义）一键发送；
- 指标回答渲染为结果卡片，可点开"指标口径 / 核算方式 / 明细（分子/分母）"三个页签；
- 左侧历史会话侧栏，会话由后端管理（SQLite 持久化）。

## 7. 模型配置

注册项：`ollama-qwen3`（4B）、`ollama-qwen3-8b-thinking`、`aliyun-qwen3-14b`、
`deepseek-v4-flash`、`deepseek-v4-pro`。模型只参与 Planner、允许列表内消歧、目标一致性
审核、Replanner 和 Final Answer；回答模板按需从
`backend-java/src/main/resources/answer-templates/` 加载。

## 8. 历史事故与已知坑（务必阅读）

### 8.1 DBHUB_BIZ_MCP_URL 脏环境变量（复发三次，已彻底断根）

- 现象：指标结果异常偏小（如 0/27、不到 1 秒返回），日志报 "Unable to access DBHub MCP"。
- 根因：IDE 进程环境块残留 `DBHUB_BIZ_MCP_URL=http://127.0.0.1:5420/http/mcp`（该端口无服务），
  所有子终端/后端进程继承它并覆盖 yml 里的医院地址；抽取失败曾被静默吞掉，查询命中中间表旧数据。
- 断根（已实施，勿回退）：yml 占位符改名 `WIKI_DBHUB_BIZ_MCP_URL`（后端不再读通用变量名）+
  dev-run.ps1 启动提示 + 抽取失败随结果透传警告（§6.2）。
- 排查口诀：结果异常偏小或秒回 → 先看 `wiki-agent.log` 抽取日志 → 再查进程环境变量。

### 8.2 其他坑

- devtools 热重启不刷新环境变量（§5.1）。
- `field_contract.yaml` 中 `dept_id` 类型不匹配会导致 `FIELD_PRECHECK_FAILED`，字段类型必须与源库一致。
- vue-tsc 报 TS6133（未使用变量）必须删除变量本身，不要用注释压制。
- Windows 下 `pwsh -Command "内联脚本"` 会因 `$env:`、引号转义出错；复杂命令一律先写 .ps1
  文件再 `pwsh -NoProfile -File` 执行。
- 工作区根目录会反复再生 `step*.png` 截图文件，**每次提交前 `git status` 检查并删除**。
- Git add 时的 CRLF/LF 警告是良性的。
- 批量执行并发必须保持 1（§6.1）。

## 9. Git 仓库注意事项（重要）

- 外层仓库：`origin = https://github.com/KK456701/wiki-project.git`，分支 `main`。
- **`backend-java\` 内嵌独立 `.git` 指向公司 TFS，绝对不要对它 push**；日常提交都在外层仓库。
- 每完成一批修改：中文提交信息、只提交源码；不提交构建产物
  （`backend-java/src/main/resources/static/`）、截图、日志、`runtime/`、`tmp/`（已 gitignore）。

## 10. 近期主要提交（由新到旧）

| 提交 | 内容 |
|---|---|
| `7dec57b1` | 抽取地址改用 WIKI_DBHUB_BIZ_MCP_URL 专属变量；抽取失败全链路透传"基于旧数据"警告 |
| `5ee87ccb` | 启动脚本纠正被覆盖的抽取地址；后端日志落盘 |
| `0f0a63b4` | 指标回答改为结果卡片（口径/核算方式/明细三页签） |
| `44846bf6` | 欢迎页改为引导式面板（指标多选+时间下拉一键发送） |
| `5aa83986` | spring-boot-devtools 热重启 + dev-run.ps1 开发流程 |
| `c8bf1698` | 统一抽取链路接入 SyncDataService，MRAS 查询前自动抽取 |
| `775f4eb0` | 知识库完全切换 knowledge-index-mras，移除 core-rules-wiki 运行时依赖 |
| `95dabff5` | 接入 MRAS 核心链路（模板渲染/实体页解析/参数映射/四层执行） |
| `521b5787` | 恢复同事 biz-mcp 抽取网关；计算路径改为抽取→真实库概览；时间上限一年 |

## 11. 当前已知限制

1. DBHub 和医院数据库真实验收依赖公司 VPN。
2. 部分 Profile 只有概览能力，没有已验证的科室/患者比较契约。
3. 本地 4B/8B 模型复杂语义弱于 API 模型，关键安全路径由 Java 确定性控制。
4. 监控页面、指标草稿/发布工作台和"全面实施验收"已永久移除，不要恢复。
5. 抽取失败时结果仍会返回（基于中间表旧数据）并带警告，属有意设计（不阻断查询）。

## 12. 接手后建议的第一件事

1. 启动后端：`pwsh scripts/dev-run.ps1`，确认 8765 端口就绪、无异常的抽取地址覆盖提示；
2. 用 §5.4 的 guest 回放验证计算成功（10/417 量级、约 8 秒、无 `data_freshness` 字段）；
3. 阅读顺序：本文 → `README.md` → `backend-java/README.md` →
   `backend-java/src/main/resources/answer-templates/README.md`；
4. 若结果异常偏小或秒回，按 §8.1 排查口诀处理。
