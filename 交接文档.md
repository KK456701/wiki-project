# Wiki Project 交接文档

> 更新时间：2026-07-26
> 当前分支：`main`
> 本批基线提交：`08dc7b8c`（提交总数 378；本批提交完成后应重新以 Git 为准）

## 1. 当前技术栈

```text
Vue 3 + TypeScript
        │ HTTP / SSE
Java 17 + Spring Boot 3.5 + Spring AI
        ├── HXZD Wiki：规则、Profile、SQL 和字段契约
        ├── SQLite：账号、会话、Trace、Evidence 和短期对象
        ├── DBHub sidecar：只读访问医院 SQL Server
        └── Ollama / DeepSeek / 阿里云百炼
```

Python 和 MySQL 不属于当前部署依赖。

## 2. HXZD 规则现状

- 原始来源：`core-rules-wiki/raw/company/35项核心制度指标完整提取.md`
- 指标：35 项。
- Profile：45 个。
- SQL 块：169 个。
- 当前状态：`documentation_only=42`、`draft=3`、`executable=0`。
- Java 只读取 `indexes/rule_index.json` 和每项指标的 `runtime.json`。
- Profile 独立保存状态、适用范围、四类 SQL 引用、参数、字段契约、结果映射和阻断原因。
- 当前未通过医院字段契约和统一结果列映射验证的 Profile 只能解释，不能访问 DBHub。

状态语义：

| 状态 | 行为 |
|---|---|
| `executable` | 可准备 SQL、只读试运行；声明明细契约时可查看和导出 |
| `documentation_only` | 只返回定义、公式、分子、分母和阻断原因 |
| `draft` | 不进入生效口径 |

## 3. Agent 执行边界

- 指标识别采用规则、本地字符语义和候选内 LLM。
- Planner 只输出业务 `RequestPlan`，不选择工具。
- `PlanGoalAlignmentValidator` 在 IR 前检查计划是否偏题；Replanner 只处理方向性语义错误且最多一次。
- `CapabilitySpecRegistry` 固定事实依赖、工具、参数编译和验证器。
- `ToolGateway` 是权限、超时、缓存、并发和执行门禁。
- Final Answer 只消费 `VerifiedEvidence`。
- 搜索、生效规则、候选口径、SQL、试运行和明细必须使用同一 `rule_id + profile_id`。

## 4. 永久删除

- 监控 Controller、Service、Repository、Scheduler、前端入口和 `/monitoring`。
- 指标草稿、审批、发布和治理工作台。
- `/implementation`。
- Agent“全面实施验收”的意图、IR、工具、模板和 Trace。

现有 SQLite 中历史表不做破坏性删除，但应用不再初始化或访问。

## 5. 验证结果

本批本地验证：

- Java：209 tests，0 failure，0 error，0 skipped。
- Vue：`npm run build` 通过。
- Wiki 源校验：35 指标、45 Profile、169 SQL 块。
- Wiki 生成契约校验通过。
- 仓库已清除已发现的明文数据库密码；真实库验收脚本改用环境变量。

真实库抽样仍要求：

1. 连接公司 VPN。
2. 启动 DBHub。
3. 只选择 `executable` Profile。
4. 使用人工配置的环境变量，不把凭据写入仓库。

已在历史脚本中暴露过的数据库账号应由有权限的管理员轮换。

## 6. 常用命令

```powershell
cd F:\A-wiki-project\backend-java
mvn.cmd -s .\maven-settings.xml test

cd F:\A-wiki-project\frontend-vue
npm.cmd run build

cd F:\A-wiki-project
node .\scripts\build-wiki-from-markdown.mjs --input ".\core-rules-wiki\raw\company\35项核心制度指标完整提取.md" --check
node .\scripts\validate-wiki-contract.mjs
```

启动：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\start-java-runtime.ps1 -Port 8765
```

## 7. 当前文档

- [README](README.md)
- [当前架构](docs/architecture/agent-runtime-current.md)
- [后端接口对接](docs/wiki-project_后端接口对接文档_2026-07-26.md)
- [SSE 事件](docs/wiki-project_SSE事件类型定义文档_2026-07-26.md)
- [MySQL 到 Wiki + SQLite 迁移记录](docs/migration/mysql-to-wiki-sqlite.md)

`docs/superpowers/` 和 `docs/migration/` 中的早期实施过程仅用于历史审计，不代表当前可用功能。
