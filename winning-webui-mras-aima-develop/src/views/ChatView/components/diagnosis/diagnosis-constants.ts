/**
 * 指标异常排查领域常量（动作 / 步骤 / 关卡）——ChatView 兼容入口
 *
 * 真相来源已迁移至共享层 `@/constants/diagnosis`，本文件仅作 re-export，
 * 保留 ChatView 内既有相对导入（`./diagnosis-constants`）不破坏。
 */
export * from '@/constants/diagnosis';
