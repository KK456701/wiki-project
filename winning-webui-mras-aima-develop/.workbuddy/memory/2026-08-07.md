# 2026-08-07

## 生成 AGENTS.md 与 DESIGN.md
- 在项目根目录新增 `AGENTS.md`（编码 Agent 工作约定：技术栈、命令、分层红线、查故障 readonly 边界、Do/Don't、任务入口速查）与 `DESIGN.md`（顶层设计决策与理由、领域模型、质量护栏、文档索引）。
- 定位：项目已有详尽 `docs/frontend-architecture.md`，故两份新文档聚焦「约定/红线」与「为什么这样设计」，互补不重复。
- 关键事实来源：package.json / vite.config.ts / tsconfig.app.json / eslint.config.js / src/stores/* / src/utils/{request,sse}.ts / src/monitor/index.ts / diagnosis-constants.ts / storage.ts，以及工作记忆中的 readonly 边界与 profileId===ruleId 约束。
- 提醒：.env / .env.local / *.local 已在 .gitignore；public/build-info.json 也被忽略；文档中已强调禁止提交密钥。

## 纠正：DESIGN.md 的正确定义
- 用户指出我最初把 DESIGN.md 写成了架构决策文档（应为 ARCHITECTURE.md 内容），这是错的。
- DESIGN.md 是 Google Stitch（Google Labs）2026-04 开源的「视觉设计系统规范」（google-labs-code/design.md，Apache-2.0 alpha）：控制项目"长什么样"——颜色、排版、间距、圆角、阴影、组件样式、品牌调性。
- 格式：顶部 YAML front matter 机器可读设计令牌（colors/fonts/spacing/radius/components + 语义角色）+ 下方 Markdown 人类可读设计判断（Overview/Colors/Typography/Layout/Elevation/Shapes/Components/Do's&Don'ts/Agent 指引）。可配 CLI 做 validate/compare/export（Tailwind、DTCG），含 WCAG 对比度校验。
- 已按此规范重写本项目 DESIGN.md（令牌取自 src/plugins/vuetify.ts 的 WD_COLORS + WinDesign Next 主题蓝方案）。AGENTS.md 不变（已正确指向 docs/frontend-architecture.md 作架构参考）。

## 查故障流程原型（HTML 单文件可交互）
- 交付：`prototype/查故障原型.html`（离线可用单文件，内置 mock 数据，无需后端/CDN）。
- 流程主线：① 选指标 + 日期范围 + 口径（同一指标多套口径下拉，选定后展示口径说明）→ 查询出统计数据；② 统计页显示总数/行数/已标记异常，四项快速筛选（关键字搜索、科室维度下拉、数值阈值=0/≥N、仅看已标记异常），每行可标记「缺失/增多」；③ 全屏核查弹窗（对齐现有 DataFlowDialog 形态）：左侧数据抽取→计算流程图（点击节点），右侧节点详情，含 SQL 节点支持实时编辑 + 模拟执行 + 原/新结果差异高亮对比。
- 设计依据：参考 `src/views/ChatView/components/DataFlowDialog.vue`（全屏 dialog + 左 G6 图/右详情）与 `DataFlowNodeDetail.vue`（节点类型/表/SQL 只读展示）；新需求在其基础上补「SQL 可编辑 + 立即执行 + 对比」与前面整段选择流程。
- 确认项（用户已选）：单文件可交互 HTML；快速筛选四项全要；SQL 执行=模拟执行+对比。
- 说明：原型用确定性 mock 演示「缺失/增多」可疑点（如门诊人次·口径1 内科=0、急诊科=890），核查页核心节点 o1 模拟执行后内科补回 305、急诊科修正 612，闭环展示差异。

## 查故障原型迭代：去掉异常标记列
- 第2步统计表移除「异常标记」列；标记改为**点击数据行循环切换**（未标记→缺失→增多→取消），科室列内显示小标签，行底色按 缺失(红)/增多(橙) 高亮；「进入核查」仍按已标记行数启用。

## 查故障原型迭代：去掉行标记，直接进核查
- 第2步改为**不标记、直接点「进入链路核查」**：移除行点击标记、rowtag/行高亮、已标记异常统计卡、「仅看已标记异常」筛选、按钮禁用逻辑。
- 新增蓝色提示条（info-banner）：告知用户若数据疑似缺失（人次=0）或多统计（数值偏高），可进入链路核查追溯问题节点。核查结论文案同步去掉「标记」措辞。

## 查故障原型迭代：新增「基础检查」步骤（插入步骤1与步骤2之间）
- 现 4 步：①选指标口径 → ②基础检查(3关) → ③数据确认 → ④链路核查。
- 基础检查为**顺序 3 关校验**（数据结构/事件配置/现场数值），点「开始基础检查」后依次执行；必须全部通过才能进数据确认；任一关失败则立即停止并显「重置检查」。
- 每关用 gate 卡片展示：pending→running( spinner )→pass(绿✓)/fail(红✗)，失败显示原因并停止后续关卡。
- 另加 demo 模拟下拉（默认全通过 / 第1/2/3关失败）用于预览失败即停止效果。state.gateStatus + basicCheckPassed 持久化，返回步骤不丢失已通过结果。
- 数据确认「返回上一步」改为回到基础检查（保留已通过态）；换指标/口径会重置 basicCheckPassed 重新检查。

## 查故障原型迭代：核查页执行结果视觉区分 + 分页
- 执行 SQL 后的「原结果 vs 新结果」对比改造：`runExec` 拆出 `renderExecPage`，双表用 `.ctable.orig`（灰/左灰边）与 `.ctable.new`（蓝/左蓝边）做视觉区分，顶部加 cmp-legend 说明；差异行仍按 cell-up(橙)/cell-pos(绿) 高亮。
- 新增分页：`PAGE_SIZE=5`（演示值），currentExec 保存 orig/result/isDiff/page/pageSize；行数>1 页时显示上一页/下一页 + 第 X/Y 页·共 N 行；table-wrap 限制高度可滚动。o1/d1（6行）演示 2 页，s1/e1 单行不显分页。

## 查故障原型迭代：核查页右栏加宽 + 一键导出 SQL
- 核查弹窗布局调整：左流程图改为辅助窄栏（flex:0 0 360px），右侧节点详情列变重点区（flex:1，min-width 480px）占据剩余宽度。
- 新增「⬇ 一键导出 SQL」按钮（核查页顶栏）：导出所有含 SQL 节点的【原 SQL】与【新 SQL】（新 SQL 取编辑后文本，未编辑则与原文一致），生成 txt 文件下载，文件名含指标+口径。
