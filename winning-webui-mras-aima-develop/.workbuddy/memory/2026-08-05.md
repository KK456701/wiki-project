# 2026-08-05

- 完成新对话界面「查故障」功能的点击「开始排查」之后的完整排查面板（快照驱动）。
  - 关键技术决策（已与用户确认）：profileId 直接复用用户选定的指标的 ruleId；modelId 复用 chat store 的 currentModelId（无则取首个 available）；功能范围为完整排查面板。
  - 数据层：`src/types/diagnosis.ts`（对照后端 DiagnosisCaseSnapshot）、`src/services/diagnosis.ts`（走 @/utils/request，POST/GET /diagnosis/cases 与 /cases/{id}/actions、/details，含 parseApiError 兼容多种错误结构）、`src/stores/diagnosis.ts`（快照唯一真相源，submitAction 提交后整体替换快照）。
  - UI 组件（`src/views/ChatView/components/diagnosis/`）：DiagnosisCaseCard（容器，onMounted 懒加载兜底）、DiagnosisStepTimeline、DiagnosisGateCard（三关占位+修复建议）、DiagnosisActionBar（按 currentStep 动作矩阵，约150行）、DiagnosisActionForm（从 ActionBar 抽取，case/cause/candidate 三表单）、DiagnosisEvidencePanel（CASE_INVESTIGATION 证据流）、DiagnosisDetailDialog（分子/分母明细，gate2 通过才开放）。
  - 触发链路：QuickActions.startTroubleshooting 先建/取 sessionId → createCase（/diagnosis/cases）→ loadSessionMessages → 后端写入 {{diagnosis_case:<caseId>}} 锚点；ChatMessage 用 DIAGNOSIS_CASE_RE 识别该锚点（仅 assistant 消息）异步懒加载 DiagnosisCaseCard。
  - 改动面：ChatMessage.vue、QuickActions.vue（新增 emit error / submitting / startTroubleshooting / formatDateForApi）、stores/chat.ts（导出 backendSessionId、loadSessionMessages）、index.vue（onTroubleshootError 复用 snackbar）、RuleCaliberDialog.vue（加 eslint-disable 清预存 v-html 警告）。

- 修复 Bug：`DiagnosisActionForm.vue` 的 onTypeChange 误用证据枚举 `IMPLEMENTER_SQL_REQUIREMENT` 作为候选方案「作用层+候选SQL」显示条件（该枚举是 SUBMIT_EVIDENCE 的 type，见后端契约 §4.3.7），导致 SQL_CHANGE 类修改方案无法填写 SQL。已改为 `candidateType.value === 'SQL_CHANGE'`（对齐 §4.3.10）。EvidencePanel 的同类判断本就正确，无需改。

- 校验：`npm run typecheck`（vue-tsc -b）与 `npm run lint`（--max-warnings 0）均 exit 0；A09 组件≤250行合规（ActionBar 368→约150，表单抽到独立组件）。注意 ChatMessage(340)/QuickActions(311) 为修改前已超 250 的既有文件，不在本次新增范围。

- 后端契约参考文档：`docs/异常排查-后端接口契约.md`、`docs/异常排查-前端交互流程.md`、`docs/异常排查-数据结构与状态流转.md`、`docs/异常排查功能完整流程文档.md`。BUILD_CANDIDATE type 枚举：DATA_REPAIR/EVENT_CONFIG/SQL_CHANGE/CALIBER_CHANGE；layer：SOURCE_EXTRACT/OVERVIEW。

- 修复 Bug（QuickActions.vue）：`selectedRuleIds` 声明为 `string[]`，但查故障卡片 `multipleRule:false` 使 v-autocomplete 在单选模式下把 modelValue 设成**单个字符串**，导致 `startTroubleshooting` 里 `selectedRuleIds.value[0]` 取到字符串首字符而非 ruleId，且运行时类型与声明不符。新增桥接 computed `ruleModel`：内部真相源始终是 `string[]`，`get` 按模式返回 `string|null`（单选）或 `string[]`（多选），`set` 把 Vuetify 给的字符串包成数组；模板 `v-model` 改绑 `ruleModel`。`selectedRuleIds.value[0]` 现可正确取 ruleId。typecheck+lint 均 exit 0。

- 依据 docs/异常排查-*.md 四份文档对齐「查故障」实现（用户明确要求按文档移植）。改动：
  - 自动连跑三门校验：`stores/diagnosis.ts` 新增 `advanceGates(caseId)`（按 currentStep 推导 1/2/3 关，循环 submitAction(RUN_GATE,{gate}) 直到首个 BLOCKED 或三门通过，带防死循环 guard）；`DiagnosisCaseCard.handleSubmit` 在 CONFIRM_CALIBER/RUN_BASE_CHECKS/RECHECK_GATE 后调用；`handleRecheck` 改发 RECHECK_GATE（原 RUN_GATE）；`DiagnosisActionBar` GATE 步骤移除每关手动 RUN_GATE 按钮，仅保留 RUN_BASE_CHECKS。
  - 证据表单完整对齐文档：新建 `DiagnosisEvidenceForm.vue`（summary/type/疑似层/treatment EXCLUDE-INCLUDE/expectedCaseEffect/candidateSql/validationSql/patchConditions 行编辑器/runAutomatic/requestAiAnalysis）；新建 `DiagnosisEvidenceItem.vue` 富渲染（IMPLEMENTER_SQL_REQUIREMENT 的 requirementAnalysis/sqlContext、AUTOMATIC_DATA_FLOW 的 display）；`DiagnosisEvidencePanel` 改 computed 修复提交后列表不刷新 bug。EvidenceItem 类型补 treatment/stages/display。
  - 状态色补全：STEP_STATUS_COLOR 增加 CANDIDATE_READY/SHADOW_FAILED/CANDIDATE_FAILED/DRAFT_BASELINE_EXPIRED/DRAFT_REVALIDATED/DRAFT_REVALIDATION_FAILED。
  - BUILD_CANDIDATE 表单补 requirements/expectedCaseEffect；QuickActions 创建时补 caseInput:{}。
  - 所有改动 typecheck+lint(--max-warnings 0) 均 exit 0；核心组件行数合规（DiagnosisCaseCard 250 触顶、DiagnosisEvidenceForm 243，其余远低于 250）。
  - 有意取舍：恢复机制仅靠消息锚点 `{{diagnosis_case:...}}`+onMounted 懒加载（文档另有 localStorage 双保险，本架构下锚点已足够，未加）。

- 后端 API 文档变更影响分析（winning-winex-mras-aima/docs/api/ README+diagnosis-cases/actions/hospital-drafts，及 diagnosis-troubleshooting-flow.md）：对照前端实现逐项比对。
  - 一致（无需改）：动作矩阵 11 步全覆盖、CONFIRM_*/SAVE_HOSPITAL_DRAFT 带 confirmed:true、SUBMIT_CASE/BUILD_CANDIDATE 字段、advanceGates 连跑 1→2→3、gate 编号、快照/明细类型、错误解析。
  - 偏差（需适配，已写入 plans/diagnosis-api-impact-analysis.md 待老板确认）：① 证据表单多发契约外字段 treatment/expectedCaseEffect（并从 EvidenceItem 类型移除）；② 证据 type 硬编码 IMPLEMENTER_SQL_REQUIREMENT、缺少轻量摘要形态（契约支持 4 形态）；③ AUTOMATIC_DATA_FLOW 渲染形态为臆测、契约未定义（改防御式兜底）；④ SAVE_HOSPITAL_DRAFT 未按 permissions(indicator_diagnosis_release) 门控（待确认权限来源，阻塞）；⑤ 明细按钮仅门控 gate2 状态、未按契约追加 overviewSqlHash 存在性校验。
  - 待确认决策点：证据双形态 vs 全 SQL 要求；自动取证真实 JSON 结构；权限来源。尚未改代码。

- **自行核对后端 Java 源码纠正 ③ 误判**（后端仓库 winning-winex-mras-aima/src）：
  - 后端**根本没有 `AUTOMATIC_DATA_FLOW` 这个证据 type**（src 全仓搜不到该字符串）。证据 type 仅 `IMPLEMENTER_SQL_REQUIREMENT`（触发自动生成候选+影子试跑）与「未填」两种特殊路径。
  - 自动取证由证据提交 `runAutomatic=true` 触发：`DiagnosisCaseService.submitEvidence` L422-424 `evidence.putAll(evidenceService.collect(...))`，把取证结果**扁平合并进证据对象顶层**（与 type 无关）。`DiagnosisCaseEvidenceService.collect` L60-116 真实回流字段：`summary`(=display.conclusion)、`stages`[{stage,databaseRole,sql,status(COMPLETED|NEEDS_MANUAL_EVIDENCE),rowCount?,rows?,meaning?,error?,reason?}]、`display`{found[],notFound[],unfinished[],conclusion,nextAction}、`identifierMapping`{recordType,recordIds,businessSourceField,realTargetField}、`allStagesCompleted`。
  - 仅当 type==='IMPLEMENTER_SQL_REQUIREMENT' 时后端自动附加 `requirementAnalysis`{judgement,requirement,nextAction,sqlGeneration} 与 `sqlContext`{available,layer?,layerLabel?,executableSql?,templateSqlHash?,currentResult?}。
  - 结论：display/stages **后端确有定义**（我此前报告"契约未定义、臆测"系误判，根源是旧异常排查-*.md 文档写了含 AUTOMATIC_DATA_FLOW 的4形态枚举）。前端真正的问题是 `DiagnosisEvidenceItem.vue` L59 把自动取证渲染挂在 `type==='AUTOMATIC_DATA_FLOW'`（永远不命中）→ 自动取证结果永不显示。修正方向：渲染条件改为「证据含 display/stages」。已更新 plans/diagnosis-api-impact-analysis.md 第 ③ 节、第 2 节第 2 点、修改清单第 3 项。**无需后端样例，③ 不再阻塞**。
