# 2026-08-01

- 审查并调整了 `.roo/rules/` 下 12 个规则文件的 front matter 加载策略（Roo Code rules 机制）。
  - 原问题：全部 `alwaysApply: true`（约 75KB 常驻上下文）；且 05/08/09 同时存在 `alwaysApply: true` 与 `globs`（globs 被架空为死配置）。
  - 调整后：00/01/11 维持 always（地基+行为准则）；02/03/07/10 改 glob（`src/**/*.ts`+`src/**/*.vue`）；05/08/09 改 `alwaysApply: false` 让原 globs 生效；04/06 改 description 按需（无 globs）。
  - 结论：Roo Code 中按需加载 = `alwaysApply: false`；配 globs 按文件类型触发，不配 globs 靠 description 由 AI 判断触发。

- 第二轮微调（同日上午）：把 08/09（Vue 模板简洁、组件行数限制）从 glob 改为 description 按需（去掉 globs）。原因：glob 只在"已打开匹配文件"时触发，新建组件尚未落盘时不触发；description 按任务语义触发，能覆盖创建场景。同时同步更新 `.roo/README.md`：修正"rules/ 常驻(alwaysApply)"说法、规则速查"作用域"列改为"加载方式"、模板示例改为展示按需写法、并补充加载顺序与按需策略说明。最终状态：常驻 00/01/11；glob 按需 02/03/05/07/10；description 按需 04/06/08/09。

- 第三轮（同日上午）：回答"description 按需是否可靠"的疑问，并落地修正。
  - 机制澄清：Roo Code 启动时读取所有 front matter；description 型规则的 description 会被 harness 作为"可用规则清单"自动展示给模型，discovery 自动、无需模型翻文件。真正缺口是模型"每次任务是否决定注入"是概率性的非确定行为——对硬红线不够稳。
  - 修正上轮回退：把 08/09 由纯 description 改回 `globs(src/**/*.vue)`+`description`（Auto Select+desc），此组合在 Roo 规则类型中标注"Better for new files"——现有 `.vue` 确定性加载、新建 `.vue` 靠 description 兜底。
  - 落地"always-on 规则索引"方案：把 `A00-project-conventions.md` 升级为常驻「规则索引与分派表」，逐条声明 12 个规则的加载方式与触发条件（含"Vue 创建/编辑皆触发 08"）。
  - 09（组件行数硬上限 250）按用户决定升为常驻 `always`，换取零歧义。
  - 同步更新 `.roo/README.md` 规则速查、模板（globs+description 同配说明）、历史坑记录。
  - 最终加载策略：常驻 00/01/09/11；glob(+desc) 02/03/05/07/08/10；description 04/06。教训：纯 description 按需对非确定；硬红线用 always、文件型用 glob+desc、偶发用 description+索引分派。

- 第四轮（同日上午）：优化 `A00` 的「规则索引与分派表」。

- 第五轮（同日下午）：A/B 前缀重命名后补全遗漏——(1) 规则正文内部交叉引用旧名(A00 索引表+交叉引用、A11、B05)、README 目录树 2 行；(2) 三个"规则文件列表"(README 目录树、README 速查表、A00 索引表)原按 00→11 数字序排列，未随文件名重排，与"文件名字母序 A 层在前"自相矛盾，已全部重排为真实加载序 A00→A01→A09→A11→B02→…→B10。教训：做文件名/编号重命名时，凡含该文件列表的地方(目录树、索引表、速查表、说明句)都要同步重排+改引用，不能只改文件名。
  - 排序由"按加载方式分组"改为"按文件名 00→11"排序，与 `rules/` 目录加载顺序一致、便于按文件检索；加载方式单独成列保留分组信息；去掉冗余 `#` 列（此前填行序号不映射文件名，反而碍检索）。
  - 触发条件修正：02/03/05/07/10 由"编辑"补为"创建或编辑"（新建 `.ts/.vue` 并写入代码时同样适用，glob 落盘激活后即命中）；04/06 为语义触发已涵盖，未改。
  - 结论沉淀：索引表应按文件名排序（=真实加载序+1:1映射），加载方式用列呈现；代码类规则触发条件须覆盖"创建或编辑"而非仅"编辑"。
