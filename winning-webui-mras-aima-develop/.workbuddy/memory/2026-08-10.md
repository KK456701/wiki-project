# 2026-08-10 工作日志

## 查故障原型迭代：SQL 编辑区增加 Tab 切换（直接编辑 / AI 修改）
- 核查页节点详情的 SQL 编辑区新增 `.edt-tabs` Tab：默认「✎ 直接编辑 SQL」（原 textarea），新增「🤖 AI 修改 SQL」。
- AI 修改 Tab：自然语言描述 → 「生成 SQL」→ 规则化改写 mock（`aiGenerateSql` + `appendCond`），展示生成 SQL 预览与识别说明，提供「应用到编辑器」「应用并执行」两个动作。
- 可识别意图（原型模拟，非真实大模型）：排除急诊科、改为最近30天/7天、仅统计门诊；未识别则保留原 SQL 并提示示例。
- 两 Tab 共享 `state.editedSql[nodeId]` 为唯一真相来源；切回手动 Tab 时 textarea 同步当前值。
- 文件：`prototype/查故障原型.html`。校验：JS 语法 OK，appendCond 对各节点 SQL 改写输出语法合法。

## 查故障原型迭代：源表抽取节点增加「重新抽取」功能
- 仅 `sqlKind==="源表抽取"` 的节点（s1）详情顶部新增「数据抽取操作」区与「🔄 重新抽取」按钮。
- 点击后按钮禁用 + spinner 加载 → 1s 后展示抽取结果卡片（来源 mz_jz / 落表 src_mz / 抽取记录数 / 耗时 / 完成时间 / 下游可重算提示）。
- 抽取记录数由 ORIGINAL_RESULT.s1 汇总；新增 `.re-extract-box` / `.re-extract-result` / `.spin` 样式与 `.btn:disabled` 通用样式。
- 校验：JS 语法 OK，仅 1 个源表抽取节点。

## 查故障原型迭代：单节点执行 → 整体执行（按完整链路）
- 移除每个 SQL 节点详情里的「▶ 执行 SQL」按钮（不允许单节点执行）；核查页顶栏新增「▶ 整体执行」「🔄 重置执行」按钮。
- 整体执行 `runWholeChain()`：按 `flowNodes` 链路顺序对所有含 SQL 节点执行（用各自 editedSql），置 `chainExecuted=true`，按钮禁用+「正在按链路执行…」过渡 ~1s。
- 结果呈现（清晰直观，适配长链路/大数据量）：
  1. 流程节点执行后加状态徽章 `execBadgeHtml`：差异 N 行（橙）/ ✓ 已执行（绿）；
  2. 未选中节点时显示「执行结果总览」`showNodeSummary()`：统计「共 X 节点 · Y 存在差异 · 累计差异 Z 行」，逐节点可点击行进入对比；
  3. 选中节点 `renderNodeComparison()` 复用原/新双表视觉区分 + 分页逻辑（`renderExecPage` 不变）。
- 「AI 修改 SQL」的「应用并执行」改为只应用 SQL 并提示去点整体执行；「重新抽取」保留（属源头刷新，非单节点 SQL 执行）。
- 校验：JS 语法 OK，execBtn/runExec 引用已全部清除。

## 查故障原型迭代：在「执行 SQL」原位置新增「🔍 校验 SQL」按钮
- 需求：单节点执行已移除，但需在对应位置加「校验 SQL」按钮，防止用户手输或 AI 生成的 SQL 存在语法错误/安全风险。
- 位置：①「直接编辑 SQL」面板（原执行按钮处，与「重置」并列）→ `validateBtn` + `validateArea`；②「AI 修改 SQL」结果卡 action 区新增 `aiValidateBtn` + `aiValidateArea`（校验 AI 生成 SQL）。
- 实现：`validateSql(sql)` 返回 `{ok,errors[],warnings[]}`，覆盖 8 类检查：括号/单引号平衡、起始关键字、高危 DDL（DROP/TRUNCATE/ALTER/GRANT）、数据变更（INSERT/UPDATE/DELETE）、文件写出（INTO OUTFILE）、多语句（分号）、注释绕过、UNION/注入探测函数；`renderValidateResult(r)` 渲染绿/红结果卡。点击后按钮禁用 + spinner ~600ms 再出结果。
- 顺手把 AI 结果卡「应用并执行」改名为「应用」（单节点执行已禁用，更准确）。
- ⚠️ 修复 bug：注入/UNION 两条 `if(/regex/)` 漏写 `.test(s)`，导致每条 SQL 都误报；已补 `.test(s)`，现正常 SQL 无误报、危险 SQL 被拦。
- 校验：JS 语法 OK；用原型真实代码跑 10 类样本，断言全部通过。

## 查故障原型迭代：第3步数据确认增加「数据澄清」功能
- 需求：第3步数据确认需支持澄清。①数据多了：勾选统计表中不应出现的行 → 要求澄清（解释为何被计入）；②数据少了：填写缺失记录的科室/日期/说明 → 要求澄清（解释为何未被计入）；③澄清结果可「不满意，进入链路核查」或「确认无异议」；④也可不澄清直接进入链路核查。
- 实现：统计表新增勾选列（事件委托 `#statTable` change → `state.clarifyExtra` Set，键为行索引）；新增 `.clari` 面板，含两个 Tab（`clari-tab` extra/missing）切换两个面板（标记多余数据 / 补充缺失数据）；「要求澄清」生成 mock 澄清说明（`clarifyExtraBtn`/`clarifyMissingBtn` → `showClarifyResult`），引用 `state.dateRange` 与 `state.caliberName`；结果卡含 `crToCheck`/`crOk` 两按钮。
- `queryBtn` 点击时采集 `state.dateRange = dateStart ~ dateEnd`；`enterDataConfirm` 重置 `clarifyExtra` 与 `clariResult`。原「进入链路核查」按钮保留（直接进核查，无需先澄清）。
- 校验：JS 语法 OK，所有新元素/函数连线齐全。

## 查故障原型迭代：数据澄清改为双区块并存（非互斥 Tab）
- 用户反馈：数据多了/数据少了应可同时存在，原 `clari-tab` 切换会暗示互斥。改为两个并排 `.clari-block` 区块（数据多了 = 勾选行 + 橙色 cb-tag；数据少了 = 文本域 + 红色 cb-tag），同时可见、互不排斥。
- 数据少了简化为**单个文本域** `missNote`（去掉原 科室/统计日期/补充说明 三字段表单）：点「要求澄清」校验非空即可，澄清结果直接引用用户描述原文。
- `showClarifyResult` 改为 `appendChild`（每类澄清结果独立追加，可并存查看），各自携带 crToCheck/crOk；`enterDataConfirm` 重置时清空 `missNote`。
- 校验：JS 语法 OK；clari-tab/clari-panel/missing-form/missDept/missDate 等旧引用已全部清除。

## 查故障原型迭代：步骤条支持跳过直达（第1步必选，其余可跳）
- 需求：4 步中第 1 步（选择指标与口径）为必选；完成后步骤 2/3/4 可点击步骤条直接跳过直达，无需依次经过。
- 实现：`step1Done()` 以 `state.caliberId` 是否设置判断；新增 `showBaseView(name)` 统一切换 view1/view2/viewBasic（替代各处硬编码 display）；`lastBaseView`+`VIEW_STEP` 记录底层视图，关闭核查弹窗 `closeOv` 时回到对应基础视图并 `setStep`；`jumpToStep(n)` 处理直达（n=4 直接 openCheck，其余先收起 overlay 再各自入口）；`refreshStepper()` 在 step1 未完成时给步骤 2/3/4 加 `.locked` 灰态 + toast 拦截，完成后解锁；步骤条加 `.step-tip` 说明文案；`goBasic`/`enterDataConfirm`/返回按钮改用 `showBaseView`。
- 校验：JS 语法 OK；showBaseView 引用 7 处，refreshStepper 初始化 + queryBtn 后各调用一次。

## 查故障原型迭代：第3步数据确认改为「分子/分母」双列 + 患者明细展开
- 需求：第3步统计表改为展示分子、分母数值；点击分子 / 分母数值可展开对应的患者明细列表（默认收起），明细列：患者姓名、encounterId、科室、病区；患者数据多需分页；筛选条件同步调整。
- 数据模型：`buildRows(indicator,caliberId)` 重写，每行含 `num`(分子)/`den`(分母)；新增 `INDICATOR_META` 定义各指标分子分母名（门诊人次=就诊/挂号人次；住院死亡率=死亡/出院人数；药占比=用药/就诊患者数）。口径异常仅改写分子（缺失=0 / 增多=高值），分母按语义放大且保持合理，注入 `flag=miss|more`。
- 患者明细：新增 `genPatients(dept,date,kind,count,seed)` 确定性 mock 生成（按科室映射病区）；`detailRowHtml` 渲染明细表 + `pagerHtml` 窗口分页（每页8，上一页/页码/下一页，末页禁用）。明细缓存 `state.patientCache`，展开状态 `state.openDetail`(`idx:kind`，同行仅展开一种)，页码 `state.detailPage`。
- 交互：`#statTable` click 委托 → `toggleDetail`（metric-link 展开/收起）/ `pd-close`（收起）/ `pg-btn`（翻页）；展开行以 `<tr class="detail-row">` 嵌入主表（colspan=5）。
- 筛选调整：新增「指标维度」下拉（全部/仅分子/仅分母），原「数值/阈值」改为作用于所选指标（仅看缺失=0 / ≥值）；关键字、科室下拉保留。概览统计条改为「分子合计 / 分母合计 / 明细行数」。
- 校验：JS 语法 OK；逻辑测试（stub DOM）确认 12 行、缺失/增多标记、患者字段完整、分页末页禁用均正确。

## 查故障原型修正：第3步改为「顶部卡片点击驱动」患者明细（非逐行展开）
- 用户反馈上一版（逐行点分子/分母展开）不对。正确逻辑：顶部 3 张统计卡片 = **数值(分子÷分母 %)**、**分子**（可点）、**分母**（可点）。点「分子」→ 下方明细表展示**全部分子患者**（数量=分子卡片值 sums）；点「分母」→ 展示**全部分母患者**。示例：分子20/分母100 → 数值20%，点分子出20个患者、点分母出100个患者。
- 明细表列：患者姓名、encounterId、科室、病区；默认收起（detail-panel 隐藏），点卡片展开、再点收起、点另一卡片切换；大数据量分页（每页8）。
- 实现：删除原逐行 `metric-link`/`detailRowHtml`/`toggleDetail`；新增 `state.detailKind`(null|num|den)+`getFiltered()`(卡片与明细共用筛选)；`renderDetailPanel()` 按当前筛选聚合各 (科室,日期) 的 `genPatients` 患者（缓存 key=`dept|date|kind`），总数=卡片值；`panelPagerHtml` 分页；`toggleCard` 切换；卡片 `.active` 高亮。
- 统计表保留勾选列（数据澄清用），列改为 勾选/科室/日期/数值/分子/分母（数值=分子÷分母）。
- 筛选调整：原「指标维度 全部/仅分子/仅分母」→「筛选指标 分子/分母/数值(%)」；阈值 全部/仅缺失(=0)/≥值；概览卡片随筛选范围联动（过滤后分子分母合计变化）。
- 校验：JS 语法 OK；逻辑测试断言 num明细总数==sum(分子)、den明细总数==sum(分母)（门诊人次c1：3432 / 3720），患者字段与分页正常。

## 查故障原型精简：第3步删除「科室列表表格」及其筛选条件
- 需求：用户要求删除原来的科室列表（表格列：科室、统计日期、数值、分子、分母）及其筛选条件，直接删除。
- 删除内容：① 统计表 `#statTable`（含勾选列、数值/分子/分母列、tbody `statBody`）；② 「快速筛选」工具栏 `#filters`（关键字 kw / 科室下拉 deptFilter / 筛选指标 metricSel / 阈值 threshSel+threshVal）；③ 顶部 info-banner；④ 数据澄清中的「数据多了」区块（原依赖表格行勾选 `clarifyExtra`，删表后无意义，连同 `clarifyExtraBtn`/`updateClarifyCount`/`statTable` change 委托一并移除）；⑤ 相关死 CSS（.filters/.chk/.flag-miss/.flag-more/.col-sub/.metric-link*/.detail-row/.row-check）。
- 保留：顶部 3 张卡片（数值/分子/分母，分子分母可点击驱动患者明细）、患者明细面板（卡片点击展开、默认收起、分页）、数据澄清「数据少了」区块（独立文本域，不涉及表格）。
- 代码清理：`getFiltered()` 删除，`renderStats` 仅算卡片合计 + 调 `renderDetailPanel`；`renderDetailPanel` 直接用 `state.rows` 聚合明细；`enterDataConfirm` 去掉筛选/dropdown/numSub/denSub 重置。
- 校验：JS 语法 OK（1034 行）；DOM-stub 冒烟测试确认 num明细=3432/den明细=3720、分页每页8行、面板正常填充。

## 查故障原型修正：恢复「数据多了」澄清（改从患者明细勾选）+ 明细收起提示
- 用户反馈上一版删「数据多了」错了：该区块要保留，但选择方式改为**从患者明细列表勾选多了的患者**（不再依赖已删除的科室表）。并指出「收起」不应移除节点（可再次点击展开），且初始未选分子/分母时要提示「请先选中分子或分母」。
- 恢复 `#clari` 内「数据多了」区块：`clarifyExtraBtn` + `clarifyExtraCount`（已选 N 位患者），语义改为「在患者明细列表中勾选不应被统计的患者」。clari-head 文案改为两类并存说明。
- 患者明细表新增勾选列（`.pat-check`，data-kind/data-enc），每行勾选 → `state.clarifyExtra`(`${kind}:${enc}`)；`dpBody` change 委托 + `updateClarifyExtraCount()` 联动按钮 disabled。勾选跨分页/切换分子分母均按 key 记忆。
- 明细面板不再隐藏：`renderDetailPanel` 在 `state.detailKind` 为 null 时显示 `#dpEmpty` 提示「请先选中分子或分母」并隐藏表格；点卡片 → 显示表格；点「收起」→ 回到提示态（勾选保留，可再次点卡片展开）。
- 恢复 `clarifyExtraBtn` 处理逻辑：`clarifyExtraCount` 样本展示 + 链路溯因（s1 抽取未排除 → o1/d1 聚合纳入），结果卡 crToCheck/crOk 并存。`enterDataConfirm` 重置 `clarifyExtra` 并调 `updateClarifyExtraCount`。
- 校验：JS 语法 OK；DOM-stub 测试通过（初始提示可见/表格隐藏/计数0/按钮禁用；选分子出3432人+勾选列；勾选计数1且按钮启用；收起后 set 保留、再展开勾选仍选中）。

## 查故障原型迭代：患者明细支持按姓名 / encounterId / 科室筛选
- 需求：患者明细列表支持按 患者姓名、encounterId、科室 进行筛选。
- 实现：明细面板 `#dpFilters` 新增筛选条（姓名 text / encounterId text / 科室 select + 重置按钮），仅在展开表格（`state.detailKind` 非 null）时显示，未选分子/分母时隐藏。
- `renderDetailPanel()` 新增筛选逻辑：先聚合全部患者 → 用 `state.patFilters` 按姓名(包含,忽略大小写)/encounterId(包含)/科室(精确) 过滤 → 过滤后总数驱动分页；`dpCount` 显示「筛选出 X / Y 条」（已筛选时）或原合计文案；无匹配显示「无匹配的患者，请调整筛选条件」。
- 科室下拉选项由「未筛选的全量科室集合」(`Set` + sort) 重建，避免按姓名/ID 筛选时选项跳动；当前选择若仍在集合中则保留。
- 事件：`patName`/`patEnc` input、`patDept` change → `applyPatFilter()`（重置到第1页）；`patReset` 清空三项并重渲染。`toggleCard` 与 `enterDataConfirm` 进入/切换时清空 `patFilters` 与输入框。
- 校验：JS 语法 OK；DOM-stub 断言全部通过（无筛选=5行；科室=内科→2行；姓名→1行；encounterId→1行；无匹配→0行；未选择器隐藏 dpFilters）。

## 查故障原型迭代：合并「数据多了 / 数据少了」的澄清按钮与结果
- 需求：原本两块澄清各有独立按钮与独立结果卡（数据多了=勾选患者后 clarifyExtraBtn；数据少了=文本域后 clarifyMissingBtn），现合并为一个「要求澄清」按钮 + 一张合并结果卡。
- 实现：删除 `clarifyExtraBtn`/`clarifyMissingBtn` 及其两个 handler；新增 `#clarifyAllBtn`（合并操作区 `.clari-actions`）。`clarifyAllBtn` handler：读 `state.clarifyExtra`(数据多了勾选) 与 `missNote`(数据少了描述)；若两者皆空 → toast 提示并返回；否则按各自有/无生成 `cr-seg` 分段（数据多了段用 cb-tag.more / 数据少了段用 cb-tag.miss），拼进一张 `.clari-result` 卡片；`cr-head` 标题随实际选择标注「数据多了 / 数据少了 / 数据多了 + 数据少了」。
- `showClarifyResult` 改 `box.innerHTML=` 覆盖（不再 appendChild），保证每次澄清只显示最新的合并结果；`updateClarifyExtraCount` 去掉对已删按钮的 `.disabled` 引用。`dpEmpty` 提示文案同步改为「于下方『要求澄清』按钮统一发起澄清」。
- 新增 CSS：`.cr-seg` / `.cr-seg-head` / `.cr-seg-body`（每段独立边框+浅蓝头），`.clari-actions`（合并按钮行）。
- 校验：JS 语法 OK；DOM-stub 逻辑测试 11 项全过（空选择 guard、仅数据多了、仅数据少了、两者并存=2分段且标签齐全、重新澄清仅1张卡片）。

## 查故障原型迭代：数据澄清「数据多了」增加「清空」按钮
- 需求：数据澄清面板的「数据多了」区块已显示「已选 N 位患者」，需在此基础上增加清空功能。
- 实现：① 在 `clarifyExtraCount` 旁新增 `#clearExtraBtn`（`.btn.sm.ghost`，复用既有 ghost/sm 样式）；② `updateClarifyExtraCount()` 末尾加 `$("clearExtraBtn").disabled = n===0`（已选为0时禁用，>0时启用）；③ 新增 `clearClarifyExtra()`：当 `state.clarifyExtra.size===0` 时提前返回，否则置空 Set、刷新计数、调 `renderDetailPanel()` 同步患者明细勾选态；④ 绑定 `clearExtraBtn` click → `clearClarifyExtra`。
- 校验：JS 语法 OK；DOM-stub 逻辑测试 7 项全过（勾选2位文案/按钮启用、清空后集合空+明细重渲染+文案0+按钮禁用、空集合再清空不抛错）。

## 查故障原型迭代：澄清结果「不满意」进入核查页的预设
- 需求：用户在「数据确认」第3步点澄清结果的「不满意，进入链路核查 ›」(`crToCheck`) 进入链路核查页后，应：① 默认选中**源表抽取 SQL 节点(s1)**；② 激活「🤖 AI 修改 SQL」tab（覆盖默认的「直接编辑 SQL」）；③ 把第3步的澄清文字（数据多了/数据少了，具体哪里）带入 AI 文本域 `aiPrompt`。
- 实现：`openCheck(opts)` 新增 `fromClarify` 参数；`crToCheck` 绑定改为 `openCheck({fromClarify:true})`（普通「进入链路核查」`toCheckBtn` 与步骤条直达仍走默认无预设）。fromClarify 时：`state.selectedNode="s1"`、`state._pendingAiPrefill=buildClarifyPrefill()`，并在 `renderFlow()` 后显式 `showNodeDetail(s1)`（注：`renderFlow` 本身只渲染节点列表、不渲染详情面板，必须显式调用）。
- `showNodeDetail(n, opts)` 新增 `opts.defaultTab`/`opts.aiPrefill`，并在 Tab 切换监听器后用 `state._pendingAiPrefill`（或 opts）决定初始 active tab：`initTab = opts.defaultTab || (state._pendingAiPrefill ? "ai":"manual")`，据此 toggle `.edt-tab` active、`panelManual/panelAi` 显隐，并 `aiPrompt.value=prefill`；`state._pendingAiPrefill` 一次性消费置 null（避免后续切节点/整体执行误填充）。
- 新增 `buildClarifyPrefill()`：汇总 `state.clarifyExtra`（`${kind}:${enc}`，按 num/den 分分子侧/分母侧列出 encounterId，最多10个+「等」）与 `missNote` 文本框原文，生成自然语言：指向源表抽取(s1) 节点的「【数据多了】请在 s1 抽取 SQL 增加排除条件」「【数据少了】检查 s1 时间窗口/过滤条件是否未覆盖」。
- 校验：JS 语法 OK；整体脚本 DOM-stub 集成测试 14 项全过（fromClarify→选中s1+AI tab active+面板显隐正确+aiPrompt 含【数据多了】【数据少了】+具体 enc + 分子/分母侧区分；普通 openCheck→不选中s1/不填充/ prefill 已消费）。

## 查故障原型迭代：新增「AI 初诊」+ 「数据多了」补充说明文本域
- 需求：① 在数据概览（数值/分子/分母）卡片**下方**增加「AI 初诊」，扫描统计数据中疑似测试数据（科室名、病区名、患者名含「测试 / test」字眼），提醒可能为测试数据；② 「数据多了」区块下方增加一个文本域（补充说明为何这些患者不应被统计）。
- 实现-AI初诊：新增 `#aiDiag` 容器（置于 `.stat-strip` 之后、明细面板之前）。新增 `renderAiDiagnosis()`：遍历 `state.rows` 的 分子/分母 两侧，按 `dept|date|kind` + `seedFor` 复用 `genPatients` 生成全量患者，对 name/dept/area 用 `/测试|test/i` 匹配；命中按「患者姓名/科室/病区」三类分段（每段列具体 姓名/enc/科室/病区 + 分子/分母归属，最多8条+省略），warn 态带「疑似测试数据 N 条」徽标；无命中显 ok 态「未发现疑似测试数据」。在 `enterDataConfirm` 末调用（人口不随步骤变动，单次即可）。
- 实现-测试数据注入（演示用）：`genPatients` 末尾对 **分母** 且 count>=100 且 `seedFor(key)%4===0` 的明细，确定性地把末尾 1~2 位替换为测试患者（mode 0:姓名含「测试」/ 1:科室=「测试科」/ 2:病区含「test」），**替换不增总数**。使 AI 初诊在默认场景可命中演示。
- 实现-数据多了文本域：在「数据多了」区块新增 `#extraNote`（`.extra-textarea`，rows=2，placeholder 说明为何不应计入）。`enterDataConfirm` 重置清空；`clarifyAllBtn` handler 读取并在「数据多了」cr-seg 末尾追加「你的补充说明：...」；`buildClarifyPrefill()` 也把 `extraNote` 原文并入【数据多了】段，使澄清结果卡与核查页 AI 预填同步带说明。
- 新增 CSS：`.ai-diag`(.ok/.warn)、`.ai-diag-head`、`.ai-warn-badge`、`.ai-badge.ok`、`.ai-seg`/`.ai-seg-head`/`.ai-tag`/`.ai-seg-body`/`.ai-item`、`.extra-textarea`。
- 校验：JS 语法 OK（1268 行）；DOM-stub 集成测试 7 项全过（三指标分母均存在注入测试患者、注入总数>0、genPatients 长度==count 不变、默认场景 aiDiag=warn 且含「疑似测试数据」+具体 enc 记录、无命中场景 aiDiag=ok）。

## 查故障原型迭代：第1步按钮改名 + 第2步进入即自动开始基础检查
- 需求：① 第1步「选择指标与口径」底部按钮文案「查询指标统计数据」→「开始排查」；② 点击进入第2步基础检查后，应**默认自动开始** 3 关校验，不再需要用户手动点「开始基础检查」。
- 实现：① HTML `#queryBtn` 文案改为「开始排查」；② `goBasic()` 的「未通过」分支不再显示 `startCheckBtn`，改为直接 `startBasicCheck()`（自动跑检查）；③ `resetBasicCheck()` 也改为直接 `startBasicCheck()`（重置后自动重跑）；④ `#startCheckBtn` 默认 `display:none`（仍保留在 DOM，因 startBasicCheck/goBasic 引用其 `.style`）；⑤ 顶部说明 hint 改为「进入第2步后系统将<b>自动</b>依次执行 3 关校验（无需手动点击）」。
- 校验：JS 语法 OK（1259 行）；DOM-stub 测试 4 项全过（HTML 按钮文案=开始排查；进入第2步 startCheckBtn 隐藏；第1关立即 running=自动开始；basicResult 未显示）。

## 查故障原型迭代：「数据多了」允许仅填说明（不勾选患者）
- 需求：数据多了「可以不选患者，仅输入说明」（如「需排除外科全部患者」）。即：当且仅当「未选患者 且 未填补充说明」时，才表示不存在数据多了的情况。
- 语义变更：`extraNote`（数据多了说明文本域）提升为「数据多了」的独立来源；`hasMore = clarifyExtra.size>0 || extraNote!==""`，`hasLess = missNote!==""`。澄清可发起条件 = `hasMore || hasLess`；两者皆空才 toast 阻断。
- 改动点：`clarifyAllBtn` handler 前置计算 `extraNote/hasMore/hasLess` 并改 toast 文案；「数据多了」cr-seg 条件由 `extraKeys.length>0` 改为 `hasMore`，且仅勾选时为「共 N 位…」副标题、仅说明时为「你说明的『数据多了』情况（未勾选具体患者）」；tag 用 hasMore/hasLess 判定；`buildClarifyPrefill()` 的「数据多了」段条件同步改为 `extraKeys.length>0 || extraNote!==""`，未勾选时文案改为「请排查并排除不应被统计的数据（未勾选具体患者）…」；dpEmpty 与数据多了 hint 文案补充「勾选或填写说明任一存在即视为数据多了」。
- 校验：JS 语法 OK（1272 行）；DOM-stub 集成测试 17 项全过（note-only 仍生成数据多了分段+正确副标题+含原文+不阻断；空场景阻断 toast；患者-only/患者+note/仅数据少了 分段正确；AI预填 note-only 含【数据多了】+未勾选提示+原文；空场景 AI预填不含【数据多了】）。



