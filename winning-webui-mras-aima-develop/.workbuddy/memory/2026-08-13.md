# 2026-08-13 工作记录

## 数据链路核查界面（StepLineage）呈现优化
- 优化 `src/views/DiagnosisWorkspace/components/StepLineage.vue`（左 G6 图 / 右节点详情）。
- 复用 `DATA_FLOW_LEGEND_ITEMS` 常量取代原文件内手写的 `LEGEND_ITEMS`（消除 B00 重复）。
- 图区新增 MD3 头部：标题「数据链路图」+ 节点计数 chip + 「适应画布」按钮（fitView）+ 节点快速定位下拉 `v-select`（键盘可达，与画布高亮双向同步）。
- `useDataFlowGraph` 增加 `selectNode()` 与 `ResizeObserver`（容器尺寸/响应式断点变化时自适应重绘）。
- 右侧 `DataFlowNodeDetail` 升级：空态图标+引导文案；头部加节点类型语义色 chip（`DATA_FLOW_NODE_TYPE_COLOR`）。
- 新建 `src/views/ChatView/composables/useDataFlowActions.ts`（`useReExtract`），抽取 StepLineage 与 `DataFlowDialog` 完全重复的「重新抽取」逻辑。
- `DataFlowNodeMeta` 改用 `DATA_FLOW_NODE_TYPE_COLOR`（替换脆弱的 label 匹配）。
- 校验：`npm run lint`（0 警告）、`npm run typecheck` 均通过；StepLineage 控制至 249 行（A03 ≤250）。
