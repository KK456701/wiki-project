# 2026-08-06 工作日志

## 查故障功能 前后端对照审计（仅出文档，待确认后修改）

- 审计对象：当前前端 `winning-webui-mras-aima`（workspace）。对照：后端 `winning-winex-mras-aima`、readonly 参考前端 `readonly/winning-webui-mras-aima`。
- 产出文档：`查故障功能审计清单.md`（位于项目根目录）。
- 已逐行核实的关键结论：
  - A1 前端 `DIAGNOSIS_ACTION` 仅 13 个动作，缺后端要求的 `REVISE_CANDIDATE`（后端 14 个）→ 影子试跑失败后无法修订，流程卡死。
  - A2 `advanceGates` 在 `CONFIRM_CALIBER` 后若后端未把 currentStep 推进到 GATE_1_SCHEMA，会立即 return，三关永不自动跑（需与后端确认 currentStep 推进行为）。
  - A3 `QuickActions.vue:160` `profileId = ruleId` 硬编码，未暴露 profile 选择。
  - A4 `ChatMessage.vue` 的「导出诊断报告/明细/对比」按钮调用空 TODO 桩 `handleExportAction`，点击无反应。
  - C1 候选 SQL / 影子试跑在 `DiagnosisCaseCard` 以原始 JSON（pretty）渲染，缺结构化对比 UI。
  - C2 无 hospital-drafts 查看/导出入口（SAVE_HOSPITAL_DRAFT 成死胡同）。
  - B 级：明细 409（DIAGNOSIS_DETAIL_CONTEXT_MISSING / CONTRACT_CHANGED）未分支；request() 无超时/重试；明细缓存永不过期；modelId 被后端改写；releaseResult 恒空。
  - D 级：动作参数用 string 未收窄枚举；isDetailAvailable 依赖可选 overviewSqlHash；Timeline 未知步骤静默。
- 待用户确认边界：profileId 是否需选择；医院草稿/诊断导出是否在本前端范围；影子试跑展示精细度；CONFIRM_CALIBER 后 currentStep 推进行为。
- 状态：文档已交付，等待用户确认修改范围后再动手。

## 用户澄清（14:50）：A3 非缺陷，撤回
- 用户明确：`profileId` 就是 `ruleId`（用户手动选择的指标），无需暴露独立 profile 选择。
- 业务规则：**算指标 = 指标多选；查故障 = 指标单选**。前端 `QuickActions.vue` 已用 `multipleRule`（CALCULATE=true / TROUBLESHOOT=false）正确实现，`:multiple` 绑定 `activeCard?.multipleRule`。
- 行动：文档中 A3 已由「硬缺陷」改为「已确认非问题」并移除出 A 级清单；第 2 节边界问题 #1、第 3 节 P0 优先级、第 4 节验证说明同步更新。
- 仍待确认：A2 三关自动跑（需后端确认 currentStep 推进行为）、C2/A4 医院草稿与导出范围、C1 影子试跑展示精细度。

## 三个边界的 readonly 参考实现对照（14:55，探索代理核实）
- 仓库 `readonly/winning-webui-mras-aima` 结构差异：无 `stores/diagnosis.ts`，三关推进在 `src/views/AgentChatView.vue`，草稿/导出 API 在 `src/api/agent.ts`，影子试跑结构化渲染在 `src/components/DiagnosisCasePanel.vue`，草稿查看/导出/回收在 `src/views/KnowledgeReviewView.vue`。
- 边界1（三关推进/A2）：readonly `advanceDiagnosisGates`(AgentChatView.vue:216-229) 在 CONFIRM_CALIBER 后端响应后立即调用，for(;;) 每轮重读 currentStep 并显式 RUN_GATE；同样 if(!gate) return（仍依赖后端推到 GATE_1，否则第一轮 return）。**风险**：readonly 循环无迭代上限、无 currentStep 变化校验 → 后端 PASS 后不推进会无限重跑。修复要加迭代上限+变化校验。
- 边界2（草稿+导出/C2,A4）：readonly 已闭环——诊断内 SAVE_HOSPITAL_DRAFT 落库回显 draftResult.draftId；独立路由 /knowledge-review (KnowledgeReviewView.vue) 列表/详情/回收 ZIP（exportHospitalKnowledgePackage, api/agent.ts:287-322）。诊断报告导出实装：标记 {{diagnosis_export:DDR_xxxx}} 解析 → exportDiagnosis (AgentChatView.vue:277-290) → window.confirm + createDiagnosisReportExport + downloadIndicatorExport + 权限门控 canExportDetails。
- 边界3（影子试跑 UI/C1）：readonly 三层结构化对比（DiagnosisCasePanel.vue:122-170）——①抽取数据变化(recordSetDiff: 中间表总记录数/去重业务编号/减少/新增) ②最终指标结果变化(分子/分母/结果值, originalResult vs candidateResult) ③案例编号验收(前后记录数/预期/验收)；JSON 仅折叠在 <details> 技术对账区(:809-817)。
- 行动：文档第2节三个边界问题、A2 建议已补充 readonly 对照结论。仍待用户按 readonly 做法拍定范围。

## 决策汇总 + 实施约束（15:12）
- 用户要求：列出仍需决策的点（详细）+ 我的建议；并强调 readonly UI/交互仅作参考，移植须遵循当前项目 Vuetify4/Material Design 最佳实践。
- 核实技术栈：当前项目 vuetify@^4.1.6 + vue@^3.5（用户说的 vuetify4 准确）。
- 文档新增：第5节「待决策项与建议」——决策1(C2/A4 草稿+导出范围，建议选B只做导出)、决策2(C1 影子试跑深度，建议选A完整三层)、决策3(A2 三关修复，建议选A直接上健壮版+防无限循环)；并列出无需决策明确缺陷(A1/B1/B2/B3/D3/D4/E)。第6节「实施约束」明确 readonly 仅参考、用 Vuetify4 官方组件(Material Design)重实现。
- 项目长期记忆 MEMORY.md 已创建，固化：技术栈、profileId=ruleId 约定、readonly仅参考+Vuetify4/MD 实施约束。

## 按建议执行（代码落地，续）
- 用户拍板决策 B/A/A，约束：readonly 仅参考，移植须结合当前项目做 Vuetify4/MD 重设计，非简单替换组件。
- 本次续做收尾：
  - D3 动作类型收窄：DiagnosisEvidenceForm.vue、DiagnosisEvidencePanel.vue 的 emit 由 `string` 收窄为 `DiagnosisActionName`；DiagnosisCaseCard.vue 的 `handleSubmit` 参数、DiagnosisActionBar.vue 的 `onFormSubmit` 参数同步收窄（否则传给已收窄的 store.submitAction 会编译失败）。已全量 grep 确认无其他裸 string 调用点。
  - E 系列清理（Task #9）：
    - ChatMessage.vue：移除 handleQuickAction/handleInspect 的 console.log + eslint-disable 占位，改为「功能建设中」v-snackbar 明确反馈（后端动作端点 analyze-batch / inspect-indicator 尚未接入）。
    - ChatView/constants.ts：RECOMMENDED_QUESTIONS 注释由模糊 TODO 改为明确「静态兜底」说明。
    - services/export.ts：补充反向代理部署说明——导出端点统一走 `/wiki-agent/api` 前缀，复用现有 vite 代理/生产反向代理，无需单独路由。
    - eslint.config.js：为 .ts/.vue globals 补充 `RequestInit: 'readonly'`（DOM 类型全局，globals.browser 未收录，否则 B2 的 `RequestOptions extends RequestInit` 被 no-undef 误报）。
- 校验：typecheck（vue-tsc -b）、lint（eslint --max-warnings 0）、build 全部通过。

## 新增：动作提交错误结构 `DIAGNOSIS_STEP_ORDER_VIOLATION` 处理（16:11）
- 用户给出后端真实返回示例：`{ "detail": { "message": "当前步骤是 CHANGE_PROPOSAL，不能执行 CASE_INVESTIGATION 的动作", "code": "DIAGNOSIS_STEP_ORDER_VIOLATION" } }`（动作接口 `POST /api/diagnosis/cases/{caseId}/actions`）。
- 现状缺口：(1) `submitDiagnosisAction` 之前只抛裸 `Error`（丢 code）；(2) 前端过期快照（本地 currentStep 落后于后端真实步骤）不会自愈，用户反复点同一失效按钮死循环。
- 修复：
  - services/diagnosis.ts：新增 `DiagnosisActionError extends Error { code? }`（与 DiagnosisDetailError 平行）；新增内部 `extractErrorDetail(res)` 统一解析 `{detail:string}`/`{detail:{code,message}}`/顶层 message/code，`parseApiError` 委托它；`submitDiagnosisAction` 非 2xx 抛 `DiagnosisActionError(message, code)`（不挑 HTTP 状态，400/409/422 都能拿 code）；`getDiagnosisDetails` 409 分支改用 `extractErrorDetail`（行为不变）。
  - stores/diagnosis.ts：`submitAction` 捕获 `DiagnosisActionError` 且 code=DIAGNOSIS_STEP_ORDER_VIOLATION 时，调 `getDiagnosisCase` 重拉快照整体替换本地（含 invalidateDetailsCache）自愈，再抛友好文案「排查进度已与服务器同步，请重新操作」（隐藏内部步骤名 CHANGE_PROPOSAL/CASE_INVESTIGATION）；重拉失败保留原错误。对所有走 submitAction 的调用者（含 advanceGates 的 RUN_GATE）生效。
  - DiagnosisCaseCard.vue：handleSubmit catch 识别 order-violation 改用 info 样式提示（其余维持 error）；模板 v-alert 增加 `:type="actionErrorVariant"`。
- 校验：typecheck + lint 通过。

## 新增：CHANGE_PROPOSAL 误发 SUBMIT_EVIDENCE 根因定位 + 修复（16:51）
- 用户给出在【拟定修改方案】步骤调用动作接口、入参 `action: SUBMIT_EVIDENCE`（payload 含 `type: IMPLEMENTER_SQL_REQUIREMENT`/`suspectedLayer`/`patchConditions`/`runAutomatic`），响应同上 `DIAGNOSIS_STEP_ORDER_VIOLATION`。
- 根因（对照 readonly DiagnosisCasePanel.vue 确认）：`SUBMIT_EVIDENCE` 仅属于 **CASE_INVESTIGATION** 步骤（readonly `submitEvidence()`，line 508）；【拟定修改方案】步骤用的是 **BUILD_CANDIDATE**（readonly `buildCandidate()`，line 667-675，payload 含 `type∈{DATA_REPAIR,EVENT_CONFIG,SQL_CHANGE,CALIBER_CHANGE}`/`layer`/`requirements`/`sql`/`expectedCaseEffect`）。用户这条 payload 是 CASE_INVESTIGATION 证据表单的原样产物，被塞到 CHANGE_PROPOSAL 步骤 → 后端按步骤顺序拒绝。
- 当前前端真实 bug：`DiagnosisEvidencePanel` 的「提交证据」按钮/表单**不受步骤限制**，而面板渲染条件为 `CASE_INVESTIGATION || evidence.length > 0`（DiagnosisCaseCard.vue:167）。一旦在 CASE_INVESTIGATION 提交过证据，进入 CHANGE_PROPOSAL 面板仍渲染且可再次提交 SUBMIT_EVIDENCE → 命中该错误。readonly 把两动作分在各自 currentStep 模板块，不会串。
- 修复：
  - DiagnosisEvidencePanel.vue：新增 `canSubmit = computed(() => currentStep === 'CASE_INVESTIGATION')`，「提交证据」按钮、`v-if="showForm"` 表单、空证据提示文案均按 canSubmit 门控；后续步骤（含 CHANGE_PROPOSAL）仅展示历史证据，禁止提交。
  - DiagnosisActionForm.vue：`onTypeChange` 的 showLayer 由仅 `SQL_CHANGE` 改为 `SQL_CHANGE || CALIBER_CHANGE`（对齐 readonly buildCandidate 的「只修改一层」选项；CALIBER_CHANGE 也需选作用层 + 候选 SQL）。
- 校验：typecheck（vue-tsc -b）+ lint（eslint --max-warnings 0）通过。
- 结论：本前端 CHANGE_PROPOSAL → BUILD_CANDIDATE 的实现正确；用户的 SUBMIT_EVIDENCE 调用是动作名用错（应为 BUILD_CANDIDATE，且 type 不能用 IMPLEMENTER_SQL_REQUIREMENT，要用 DATA_REPAIR/EVENT_CONFIG/SQL_CHANGE/CALIBER_CHANGE）。

## 新增：BUILD_CANDIDATE 后 currentStep 不推进（17:24）
- 用户在【拟定修改方案】调 `POST .../actions`，入参 `{"action":"BUILD_CANDIDATE","payload":{"type":"DATA_REPAIR"}}`（极简，缺 layer），出参 currentStep 始终 CHANGE_PROPOSAL。
- **契约定性（docs/异常排查-后端接口契约.md）**：§1.1 状态图与 §1.2 状态对照明确 BUILD_CANDIDATE 定义为 CHANGE_PROPOSAL → SHADOW_TRIAL（status CANDIDATE_READY）。§4.3.10 载荷示例 5 字段全列（type/layer/requirements/sql/expectedCaseEffect），校验首条即 `layer 必须是 SOURCE_EXTRACT | OVERVIEW`。故「不推进」= 候选未按校验收尾，非前端渲染挡住（store 收到新快照直接替换、BUILD_CANDIDATE 不触发 advanceGates）。
- readonly 对照：`buildCandidate`(DiagnosisCasePanel.vue:667-675) **始终发 type+layer(默认 SOURCE_EXTRACT)+requirements+sql+expectedCaseEffect 五字段**（即便空串），故 readonly 的 DATA_REPAIR 能推进。
- 当前前端 bug：`DiagnosisActionForm.submit()` 候选分支只在 SQL_CHANGE/CALIBER_CHANGE 时带 layer、且空字段直接省略 → DATA_REPAIR/EVENT_CONFIG 漏发 `layer`，与契约/readonly 不一致。
- 修复：candidateLayer 默认值改为 `'SOURCE_EXTRACT'`；候选分支改为**始终发 5 字段**（type/layer=候选层或默认 SOURCE_EXTRACT/requirements/sql/expectedCaseEffect，空串也发），对齐 §4.3.10 + readonly。
- 待用户/后端确认：DATA_REPAIR 是否需要提供 `patchConditions`（§4.3.10 SQL 生成优先级 2）或命中 deterministicCandidate 已知模式才能生成候选 SQL；以及 §4.2 允许动作正则**未含 REVISE_CANDIDATE**（A1 新增动作），需核实后端是否真支持。
- 校验：typecheck + lint 通过。

## 新增：BUILD_CANDIDATE 候选 SQL 触发 CANDIDATE_SQL_INVALID（18:22）
- 用户在【拟定修改方案】调 `POST .../actions`，入参 `BUILD_CANDIDATE` + `sql:"select * from INPATIENT_ENCOUNTER where FULL_NAME='测试学校' AND ENCOUNTER_ID=... and DISCHARGED_FROM_WARD_AT BETWEEN '2025-01-01' and '2025-12-31'"`，响应 `{"detail":{"message":"必须包含一组已登记的开始时间和结束时间参数","code":"CANDIDATE_SQL_INVALID"}}`。
- **契约定性**：
  - 错误码表（契约 §6）`CANDIDATE_SQL_INVALID | 422 | 候选 SQL 未通过只读校验`；同表 `CANDIDATE_TEMPLATE_PARAMETER_CHANGED | 422 | 候选 SQL 改变了模板参数`。
  - §4.3.10 校验第三条：**候选 SQL 必须保留原口径模板参数**。
  - dataflow-spec.md:24 模板 SQL 告警 =「SQL 仍包含未渲染模板标记」→ 原口径存的是**知识库模板 SQL**（`templateSql`/`sourceExtractSql`），时间窗口用「已登记的」模板参数表达；「可复制到 Navicat」的版本（`executableSql`/`*Executable`）才把日期渲染填进。
  - 候选 `sql` 字段是**模板版**（对应 candidateSql.sql / originalSql），不是 executable 版（契约 §7.3 `originalSqlExecutable`/`candidateSqlExecutable` 才是带日期的可执行版）。
- **根因**：用户把「可执行版」SQL（日期写死 `BETWEEN '2025-01-01' and '2025-12-31'`）当候选 SQL 提交，时间窗口的已登记模板参数对被改掉/丢失 → 只读校验器 `ReadOnlySqlValidator` 要求保留该参数对 → 拒绝。
- **给用户的正确入参**：
  - 方案 A（保留模板参数）：从案例「当前正式抽取 SQL」（模板版 templateSql，NOT Navicat 版）原样复制其时间窗口谓词，候选 SQL 仅改排查相关部分，时间窗口保持模板参数占位（文档未暴露确切 token 名，应以 UI 展示的 templateSql 为准，勿写死日期）。
  - 方案 B（更稳）：`sql` 留空 + 提供 `patchConditions`（§4.3.10 SQL 生成优先级 2 `SafeSqlPredicatePatcher` 从模板 SQL 结构化生成候选，自动保留模板时间参数）。该路径正是用户首条消息所用结构。
- **前端改进建议（尚未实施，待确认）**：`DiagnosisActionForm` 候选 SQL 文本框当前为空，用户易粘 Navicat 可执行 SQL 踩坑。建议：SQL_CHANGE/CALIBER_CHANGE 时以 `caliberSnapshot.sourceExtractSql`（模板版）预填候选 SQL 文本框 + 增加提示「请使用知识库模板 SQL，保留时间窗口模板参数，勿用 Navicat 可执行版写死日期」。属审计范围外的新发现。

## 修复：影子试跑「候选（新）」列误带 +/- 符号与颜色（19:02）
- 用户反馈：抽取数据变化表的「候选（新）」列不应显示 +x/-x（带符号+红绿），纯数字即可；带符号与颜色应只在「变化」列。
- **根因**：DiagnosisResultPanel.vue 抽取表 `候选（新）` 单元格用了 `fmt(row.shadow, row.sign)`（line 257），对「候选抽取减少/新增」两行把 sign(-/+) 拼进数字并上了红绿类；而「变化」列（258）本就该显示 deltaText。指标结果表（line 280 `fmt(row.c)`）本就是纯数字，无此 bug。
- **修复**：抽取表「候选（新）」单元格改为 `fmt(row.shadow)`（纯数字、无颜色）；符号与红绿颜色仅保留在「变化」列 `deltaText/deltaClass`（`sign` 仅用于内部 delta 计算，不再进展示）。
- **颜色约定确认**：当前 `deltaClass` = diff>0 红 / diff<0 绿，即「增加=红 +x、减少=绿 -x」，对齐用户早前指定（18:xx 消息）且契合故障排查语义（记录数减少=修复见效=绿；增加=疑似引入新数据=红）。已在用户询问时确认此约定合理。
- 校验：typecheck + lint 通过。

## 新增：影子试跑结果三层对比表增加「变化」列（18:52）
- 需求：影子试跑结果列表（`DiagnosisResultPanel.vue`）原本三层对比表均为 `变化项/指标 | 正式（原）| 候选（新）` 三列，需增加一列「变化」= 新数据对比老数据的变化；约定：**增加→红色 `+x`，减少→绿色 `-x`**（x 为绝对值，无变化显示 0/灰，无数据显示 —）。
- 实施（仅改 `DiagnosisResultPanel.vue`，未动类型定义/服务）：
  - 新增通用 helper `deltaText(diff)`（带 +/- 号）与 `deltaClass(diff)`（>0 红 text-error、<0 绿 text-success、0/null 灰）。
  - 抽取数据变化表（第一层）：新增 `变化` 列，`extractionRows` 每行算 `delta`=候选-正式（仅派生计数行如「候选抽取减少/新增」按 `sign` 语义返回带符号变化）。
  - 最终指标结果变化表（第二层）：新增 `变化` 列，resultRows 算 `delta`=c-o。
  - 案例编号验收表（第三层）：**原本已有 `变化` 列但颜色约定相反**（旧：减少=红/增加=绿），本次统一为同一套约定（增加=红/减少=绿）并改显示 `+x/-x` 带符号，三张表口径一致。
- 校验：typecheck（vue-tsc -b）+ lint（eslint --max-warnings 0）通过。
