# {{指标名称}} · 受控 SQL

> **生成状态**
> 说明 SQL 是否已生成、是否通过只读安全校验，以及本轮是否执行数据库。

## SQL 口径

先写指标定义、分子、分母、时间字段和排除条件，确保读者能理解 SQL 在计算什么。

## 已校验 SQL

使用 SQL 代码块展示服务端允许输出的 SQL；若 Evidence 只提供对象编号，则只展示对象编号，不得补造 SQL。

## 统计参数

用表格列出统计区间和已公开的安全参数。

## 安全状态

列出 SQL 对象编号、校验状态以及“仅生成”或“已试运行”状态。

不要输出占位符和本模板说明。
