# {{指标名称}}

> **口径速览**
> 用一到两句话说明指标衡量什么。只能根据 `effective_level` 判断当前采用层级：
> `company` 必须写成“公司公版口径”，`hospital` 必须写成“本院覆盖口径”，
> 不能把 company 解释成医院级，也不能写成国家口径。

## 口径摘要

用紧凑表格展示：指标定义、规则编号、规则版本、生效层级。

## 计算口径

明确写出统一公式，并分别解释分子、分母、统计时间字段、去重方式和排除条件。没有证据的项目写“当前证据未提供”，不得猜测。

> **使用说明**
> 只写与理解或使用该指标直接相关的注意事项。不要展示 knowledge governance、
> documentation_only、execution_status、字段映射状态、SQL 门禁等内部实施信息。
> 时间字段必须使用受控中文名称：`admitted_to_ward_at` 写成“首次入区时间”。
> 不要输出占位符和本模板说明。
