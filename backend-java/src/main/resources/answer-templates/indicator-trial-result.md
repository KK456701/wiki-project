# {{指标名称}} · 统计结果

> **结论速览**
> 首句直接给出指标率，并注明统计区间。

## 结果速览

| 统计项 | 结果 |
|---|---:|
| 统计区间 | {{开始时间}} 至 {{结束时间}} |
| 分子 | {{分子}} |
| 分母 | {{分母}} |
| 指标率 | **{{指标率}}%** |

在表格后写出一次完整计算过程：分子 ÷ 分母 × 100%。

如果 `no_sample=true` 或分母为 0：

- 结论必须写“统计区间内无样本，指标率不适用”；
- 指标率表格单元格写“**不适用**”；
- 不得把 `0 ÷ 0` 写成 `0%`，也不得声称指标率为 0。

如果 VerifiedEvidence 含 `comparison_status`，增加“## 双库核对”：

- 分别列出业务库与真实库的分子、分母和服务端复算指标率。
- `matched` 明确写“双库分子、分母完全一致”。
- `mismatched` 明确写“双库概览不一致”，并只转述已确认的科室/患者差异统计；
  明细契约不完整时必须说明暂不能定位具体原因。
- 不把“指标率相同”表述成“双库一致”，判断依据始终是分子和分母。

## 计算口径

用列表说明分子、分母、时间字段、去重方式和排除条件。只使用 VerifiedEvidence。
如果 `calculation_definition.time_dimension=admitted_to_ward_at`，必须写“首次入区时间”，
不得改写为“入院时间”，也不得声称规则未提供时间字段。

## 数据依据

说明规则版本、试运行对象编号和证据限制。没有分月数据时只说明未提供，不延伸趋势判断。

不要输出占位符和本模板说明。
