---
title: "衍生指标实体 - HXZD-011-001_手术医师手术时间重合率"
created: 2026-07-28
updated: 2026-07-28
type: entity
tags: ["HXZD-011-001", "手术安全核查制度", "CORE_SURGERY", "逻辑判定类"]
sources: [raw/excel/AI指标顾问【AI Metrics Advisor】 v6.xls]
confidence: high
---

# HXZD-011-001 — 手术医师手术时间重合率

> **指标概念：** [[concepts/HXZD-011-001手术医师手术时间重合率]] | **制度：** 手术安全核查制度 | **事件编码：** CORE_SURGERY
> **版本：** 公版 | **指标导向：** 逐步降低

## 指标定义

同一时间内手术医师为同一人的手术例数占同期住院患者手术总例数的比例。

## 计算公式

分子：同一时间内手术医师为同一人的手术例数
分母：同期住院患者手术总例数
公式：同一时间内手术医师为同一人的手术例数 / 同期住院患者手术总例数 * 100%

## 说明

本指标中"同一时间"是指手术未结束时间与其他手术开始时间重合。

## 指标意义

反映手术医师参与手术安全核查情况。

## 统计口径

**分子：** 同一时间内手术医师为同一人的手术例数
> 1、单位时间内，同一个主刀医师，手术时间重合的手术数量。
2、手术重合举例：A手术开始时间和结束时间区间跟任意B手术开始时间和结束时间的区间有重合。即A手术和B手术都属于重合，统计为2。
3、说明：手术患者判断时，只需要统计手术类型为手术、介入治疗，排除诊断性操作、治疗性操作的数据

**分母：** 同期住院患者手术总例数
> 单位时间内完成手术总例数

## 数据来源

| 数据层 | 表名/说明 |
|--------|----------|
| 源表 | `CLIBASIC_SURGERY, EMPLOYEE_INFO, INPATIENT_ENCOUNTER, INPATIENT_PARTICIPANT, INP_SURGICAL_ANESTHESIA_PLAN, INP_SURGICAL_OPERATOR_PLAN, INP_SURGICAL_ORDER_ITEM, INP_SURGICAL_PLAN, ORGANIZATION, ORGANIZATION_X_EMPLOYEE` |
| XXJOB作业 | T+1 |
| 目标表-概览 | `MRAS_BUSINESS_SURGERY` |
| 目标表-科室统计 | `MRAS_BUSINESS_SURGERY` |
| 目标表-患者明细 | `MRAS_BUSINESS_SURGERY` |
| 中间表 | `MRAS_BUSINESS_SURGERY` |
| 业务表(不影响数据) | `INPATIENT_ENCOUNTER, INP_SURGICAL_PLAN, MRAS_ORGANIZATION` |
| 业务表(影响数据) | `INP_SURGICAL_ANESTHESIA_PLAN` |

## 是否影响数据

**是**

## 监测参数

| 参数 | 值 |
|------|-----|
| 时间维度 | 手术完成时间 |
| 患者范围 | 在院+出院患者 |
| 数据来源 | 住院医生站 |
| 目标值 | 0.0 |
| 异常范围初筛 | > 5% |
| 方案类型 | 推荐方案（公版） |
| 配置类型 |  |
| 配置值 |  |

## 源表

```sql
SELECT 
t1.CLI_ORDER_ITEM_ID AS bizId,  --用来更新数据
  t1.SURGERY_START_AT AS eventAt, -- 预留
  GETDATE() AS extractAt,

'1' AS mrasBusinessIndexId, 
'1' AS mrasTargetDefinitionId,

'V2.0' as version, 
  t1.ENCOUNTER_ID AS encounterId,
  t12.FULL_NAME AS personName,
  t12.IMRN as imrn,
  t12.CURRENT_DEPT_ID AS deptId,
  o1.ORG_NAME AS deptName,
  t12.CURRENT_WARD_ID AS wardId,
  o2.ORG_NAME AS wardName,
  t12.ADMITTED_TO_WARD_AT AS admittedToWardAt, 
  t12.DISCHARGED_FROM_WARD_AT AS dischargedFromWardAt,  
  t13.EMPLOYEE_ID AS employeeId,
  t14.EMPLOYEE_NAME AS employeeName,
  t12.HOSPITAL_SOID AS hospitalSoid,
  t12.SOURCE_HOSPITAL_AREA_ID AS hospitalAreaId,
  CASE WHEN t1.IS_DEL = '1'  THEN 1 ELSE 0 END AS isDel,
  t1.SURGERY_START_AT AS surgeryStartAt,
t1.SURGERY_END_AT AS surgeryEndAt,
t11.SURGERY_NAME,
-- 主刀医师是否重复
    CASE WHEN EXISTS (

SELECT 1 

FROM INP_SURGICAL_PLAN t2

LEFT JOIN (

SELECT aa.CLI_ORDER_ID, MAX(ab.SURGEON_ID) AS OPERATOR_ID

FROM INP_SURGICAL_ORDER_ITEM aa

LEFT JOIN INP_SURGICAL_OPERATOR_PLAN ab ON aa.CLI_ORDER_ITEM_ID=ab.CLI_ORDER_ITEM_ID AND ab.SURGICAL_OPERATOR_ROLE_CODE = 399010952 AND ab.IS_DEL=0

WHERE aa.IS_DEL=0 

GROUP BY aa.CLI_ORDER_ID

) t20 ON t2.CLI_ORDER_ID = t20.CLI_ORDER_ID

WHERE t20.OPERATOR_ID = t10.OPERATOR_ID

AND t2.CLI_ORDER_ID <> t1.CLI_ORDER_ID

AND t2.SURGERY_START_AT < t1.SURGERY_END_AT

AND t2.SURGERY_END_AT > t1.SURGERY_START_AT
    ) THEN 98175 ELSE 98176 END AS OPERATOR_OVERLAP,
    -- 麻醉医师 是否重复
    CASE WHEN EXISTS (

SELECT 1 

FROM INP_SURGICAL_PLAN t3

LEFT JOIN (

SELECT aa.CLI_ORDER_ID, MAX(ae.SURGEON_ID) AS ANES_DOCT_ID

FROM INP_SURGICAL_ORDER_ITEM aa

LEFT JOIN INP_SURGICAL_OPERATOR_PLAN ae ON aa.CLI_ORDER_ITEM_ID=ae.CLI_ORDER_ITEM_ID AND ae.SURGICAL_OPERATOR_ROLE_CODE = 399010958 AND ae.IS_DEL=0

WHERE aa.IS_DEL=0 

GROUP BY aa.CLI_ORDER_ID

) t30 ON t3.CLI_ORDER_ID = t30.CLI_ORDER_ID

WHERE t30.ANES_DOCT_ID = t10.ANES_DOCT_ID

AND t3.CLI_ORDER_ID <> t1.CLI_ORDER_ID

AND t3.SURGERY_START_AT < t1.SURGERY_END_AT

AND t3.SURGERY_END_AT > t1.SURGERY_START_AT
    ) THEN 98175 ELSE 98176 END AS ANES_DOCT_OVERLAP,
    -- 主刀医师重复的另外一个手术CLI_ORDER_ITEM_ID
    STUFF((

SELECT ',' + CAST(t2.CLI_ORDER_ITEM_ID AS VARCHAR(50))

FROM INP_SURGICAL_PLAN t2

LEFT JOIN (

SELECT aa.CLI_ORDER_ID, MAX(ab.SURGEON_ID) AS OPERATOR_ID

FROM INP_SURGICAL_ORDER_ITEM aa

LEFT JOIN INP_SURGICAL_OPERATOR_PLAN ab ON aa.CLI_ORDER_ITEM_ID=ab.CLI_ORDER_ITEM_ID AND ab.SURGICAL_OPERATOR_ROLE_CODE = 399010952 AND ab.IS_DEL=0

WHERE aa.IS_DEL=0 

GROUP BY aa.CLI_ORDER_ID

) t20 ON t2.CLI_ORDER_ID = t20.CLI_ORDER_ID

WHERE t20.OPERATOR_ID = t10.OPERATOR_ID

AND t2.CLI_ORDER_ID <> t1.CLI_ORDER_ID

AND t2.SURGERY_START_AT < t1.SURGERY_END_AT

AND t2.SURGERY_END_AT > t1.SURGERY_START_AT

FOR XML PATH('')
    ), 1, 1, '') AS OVERLAP_OPERATOR_SURGERIES,
    -- 麻醉医师重复的另外一个手术CLI_ORDER_ITEM_ID
    STUFF((

SELECT ',' + CAST(t3.CLI_ORDER_ITEM_ID AS VARCHAR(50))

FROM INP_SURGICAL_PLAN t3

LEFT JOIN (

SELECT aa.CLI_ORDER_ID, MAX(ae.SURGEON_ID) AS ANES_DOCT_ID

FROM INP_SURGICAL_ORDER_ITEM aa

LEFT JOIN INP_SURGICAL_OPERATOR_PLAN ae ON aa.CLI_ORDER_ITEM_ID=ae.CLI_ORDER_ITEM_ID AND ae.SURGICAL_OPERATOR_ROLE_CODE = 399010958 AND ae.IS_DEL=0

WHERE aa.IS_DEL=0 

GROUP BY aa.CLI_ORDER_ID

) t30 ON t3.CLI_ORDER_ID = t30.CLI_ORDER_ID

WHERE t30.ANES_DOCT_ID = t10.ANES_DOCT_ID

AND t3.CLI_ORDER_ID <> t1.CLI_ORDER_ID

AND t3.SURGERY_START_AT < t1.SURGERY_END_AT

AND t3.SURGERY_END_AT > t1.SURGERY_START_AT

FOR XML PATH('')
    ), 1, 1, '') AS OVERLAP_ANES_DOCT_SURGERIES,
   t15.ORG_ID as bizAttrOneId,
   t15.ORG_NAME as bizAttrOneName
FROM INP_SURGICAL_PLAN t1
LEFT JOIN (
SELECT aa.CLI_ORDER_ID, MAX(ab.SURGEON_ID) AS OPERATOR_ID, MAX(ae.SURGEON_ID) AS ANES_DOCT_ID
    FROM INP_SURGICAL_ORDER_ITEM aa
LEFT JOIN INP_SURGICAL_OPERATOR_PLAN ab ON aa.CLI_ORDER_ITEM_ID=ab.CLI_ORDER_ITEM_ID AND ab.SURGICAL_OPERATOR_ROLE_CODE = 399010952 AND ab.IS_DEL=0
LEFT JOIN INP_SURGICAL_OPERATOR_PLAN ae ON aa.CLI_ORDER_ITEM_ID=ae.CLI_ORDER_ITEM_ID AND ae.SURGICAL_OPERATOR_ROLE_CODE = 399010958 AND ae.IS_DEL=0
    WHERE aa.IS_DEL=0 
GROUP BY aa.CLI_ORDER_ID
) t10 on t1.CLI_ORDER_ID = t10.CLI_ORDER_ID
LEFT JOIN (
SELECT 
    t.CLI_ORDER_ITEM_ID,
STUFF((

SELECT ',' + t2.SURGERY_NAME

FROM (

SELECT DISTINCT isa.CLI_ORDER_ITEM_ID, isa.SURGERY_NAME

FROM INP_SURGICAL_ANESTHESIA_PLAN isa

WHERE isa.IS_DEL = 0 

AND isa.SURGERY_NAME IS NOT NULL 

AND isa.CLI_ORDER_ITEM_ID IS NOT NULL

AND EXISTS (

SELECT 1 

FROM CLIBASIC_SURGERY cs 

WHERE cs.SURGERY_DIR_NO IN ('957347','957348') 

AND isa.ICD9_CM3_CODE = cs.SURGERY_NO

)

AND isa.CLI_ORDER_ITEM_ID = t.CLI_ORDER_ITEM_ID

) t2

ORDER BY t2.SURGERY_NAME

FOR XML PATH('')
    ), 1, 1, '') AS SURGERY_NAME,
    MAX(t.SURGICAL_ANESTHESIA_CODE) AS ANES_TYPE_ID
FROM (
    SELECT DISTINCT isa.CLI_ORDER_ITEM_ID, isa.SURGERY_NAME, isa.SURGICAL_ANESTHESIA_CODE
    FROM INP_SURGICAL_ANESTHESIA_PLAN isa
    WHERE isa.IS_DEL = 0 
AND isa.SURGERY_NAME IS NOT NULL 
AND isa.CLI_ORDER_ITEM_ID I
```

```sql
'SELECT 
t1.CLI_ORDER_ITEM_ID AS bizId,  --用来更新数据
  t1.SURGERY_START_AT AS eventAt, -- 预留
  GETDATE() AS extractAt,
         '1' AS mrasBusinessIndexId, 
'1' AS mrasTargetDefinitionId,
         'V2.0' as version, 
  t1.ENCOUNTER_ID AS encounterId,
  t12.FULL_NAME AS personName,
  t12.IMRN as imrn,
  t12.CURRENT_DEPT_ID AS deptId,
  o1.ORG_NAME AS deptName,
  t12.CURRENT_WARD_ID AS wardId,
  o2.ORG_NAME AS wardName,
  t12.ADMITTED_TO_WARD_AT AS admittedToWardAt, 
  t12.DISCHARGED_FROM_WARD_AT AS dischargedFromWardAt,  
  t13.EMPLOYEE_ID AS employeeId,
  t14.EMPLOYEE_NAME AS employeeName,
  t12.HOSPITAL_SOID AS hospitalSoid,
  t12.SOURCE_HOSPITAL_AREA_ID AS hospitalAreaId,
  CASE WHEN t1.IS_DEL = '1'  THEN 1 ELSE 0 END AS isDel,
  t1.SURGERY_START_AT AS surgeryStartAt,
t1.SURGERY_END_AT AS surgeryEndAt,
t11.SURGERY_NAME,
-- 主刀医师是否重复
    CASE WHEN EXISTS (
        SELECT 1 
        FROM INP_SURGICAL_PLAN t2
        LEFT JOIN (
            SELECT aa.CLI_ORDER_ID, MAX(ab.SURGEON_ID) AS OPERATOR_ID
            FROM INP_SURGICAL_ORDER_ITEM aa
            LEFT JOIN INP_SURGICAL_OPERATOR_PLAN ab ON aa.CLI_ORDER_ITEM_ID=ab.CLI_ORDER_ITEM_ID AND ab.SURGICAL_OPERATOR_ROLE_CODE = 399010952 AND ab.IS_DEL=0
            WHERE aa.IS_DEL=0 
            GROUP BY aa.CLI_ORDER_ID
        ) t20 ON t2.CLI_ORDER_ID = t20.CLI_ORDER_ID
        WHERE t20.OPERATOR_ID = t10.OPERATOR_ID
        AND t2.CLI_ORDER_ID <> t1.CLI_ORDER_ID
        AND t2.SURGERY_START_AT < t1.SURGERY_END_AT
        AND t2.SURGERY_END_AT > t1.SURGERY_START_AT
    ) THEN 98175 ELSE 98176 END AS OPERATOR_OVERLAP,
    -- 麻醉医师 是否重复
    CASE WHEN EXISTS (
        SELECT 1 
        FROM INP_SURGICAL_PLAN t3
        LEFT JOIN (
            SELECT aa.CLI_ORDER_ID, MAX(ae.SURGEON_ID) AS ANES_DOCT_ID
            FROM INP_SURGICAL_ORDER_ITEM aa
            LEFT JOIN INP_SURGICAL_OPERATOR_PLAN ae ON aa.CLI_ORDER_ITEM_ID=ae.CLI_ORDER_ITEM_ID AND ae.SURGICAL_OPERATOR_ROLE_CODE = 399010958 AND ae.IS_DEL=0
            WHERE aa.IS_DEL=0 
            GROUP BY aa.CLI_ORDER_ID
        ) t30 ON t3.CLI_ORDER_ID = t30.CLI_ORDER_ID
        WHERE t30.ANES_DOCT_ID = t10.ANES_DOCT_ID
        AND t3.CLI_ORDER_ID <> t1.CLI_ORDER_ID
        AND t3.SURGERY_START_AT < t1.SURGERY_END_AT
        AND t3.SURGERY_END_AT > t1.SURGERY_START_AT
    ) THEN 98175 ELSE 98176 END AS ANES_DOCT_OVERLAP,
    -- 主刀医师重复的另外一个手术CLI_ORDER_ITEM_ID
    STUFF((
        SELECT ',' + CAST(t2.CLI_ORDER_ITEM_ID AS VARCHAR(50))
        FROM INP_SURGICAL_PLAN t2
        LEFT JOIN (
            SELECT aa.CLI_ORDER_ID, MAX(ab.SURGEON_ID) AS OPERATOR_ID
            FROM INP_SURGICAL_ORDER_ITEM aa
            LEFT JOIN INP_SURGICAL_OPERATOR_PLAN ab ON aa.CLI_ORDER_ITEM_ID=ab.CLI_ORDER_ITEM_ID AND ab.SURGICAL_OPERATOR_ROLE_CODE = 399010952 AND ab.IS_DEL=0
            WHERE aa.IS_DEL=0 
            GROUP BY aa.CLI_ORDER_ID
        ) t20 ON t2.CLI_ORDER_ID = t20.CLI_ORDER_ID
        WHERE t20.OPERATOR_ID = t10.OPERATOR_ID
        AND t2.CLI_ORDER_ID <> t1.CLI_ORDER_ID
        AND t2.SURGERY_START_AT < t1.SURGERY_END_AT
        AND t2.SURGERY_END_AT > t1.SURGERY_START_AT
        FOR XML PATH('')
    ), 1, 1, '') AS OVERLAP_OPERATOR_SURGERIES,
    -- 麻醉医师重复的另外一个手术CLI_ORDER_ITEM_ID
    STUFF((
        SELECT ',' + CAST(t3.CLI_ORDER_ITEM_ID AS VARCHAR(50))
        FROM INP_SURGICAL_PLAN t3
        LEFT JOIN (
            SELECT aa.CLI_ORDER_ID, MAX(ae.SURGEON_ID) AS ANES_DOCT_ID
            FROM INP_SURGICAL_ORDER_ITEM aa
            LEFT JOIN INP_SURGICAL_OPERATOR_PLAN ae ON aa.CLI_ORDER_ITEM_ID=ae.CLI_ORDER_ITEM_ID AND ae.SURGICAL_OPERATOR_ROLE_CODE = 399010958 AND ae.IS_DEL=0
            WHERE aa.IS_DEL=0 
            GROUP BY aa.CLI_ORDER_ID
        ) t30 ON t3.CLI_ORDER_ID = t30.CLI_ORDER_ID
        WHERE t30.ANES_DOCT_ID = t10.ANES_DOCT_ID
        AND t3.CLI_ORDER_ID <> t1.CLI_ORDER_ID
        AND t3.SURGERY_START_AT < t1.SURGERY_END_AT
        AND t3.SURGERY_END_AT > t1.SURGERY_START_AT
        FOR XML PATH('')
    ), 1, 1, '') AS OVERLAP_ANES_DOCT_SURGERIES,
   t15.ORG_ID as bizAttrOneId,
   t15.ORG_NAME as bizAttrOneName
FROM INP_SURGICAL_PLAN t1
LEFT JOIN (
SELECT aa.CLI_ORDER_ID, MAX(ab.SURGEON_ID) AS OPERATOR_ID, MAX(ae.SURGEON_ID) AS ANES_DOCT_ID
    FROM INP_SURGICAL_ORDER_ITEM aa
LEFT JOIN INP_SURGICAL_OPERATOR_PLAN ab ON aa.CLI_ORDER_ITEM_ID=ab.CLI_ORDER_ITEM_ID AND ab.SURGICAL_OPERATOR_ROLE_CODE = 399010952 AND ab.IS_DEL=0
LEFT JOIN INP_SURGICAL_OPERATOR_PLAN ae ON aa.CLI_ORDER_ITEM_ID=ae.CLI_ORDER_ITEM_ID AND ae.SURGICAL_OPERATOR_ROLE_CODE = 399010958 AND ae.IS_DEL=0
    WHERE aa.IS_DEL=0 
GROUP BY aa.CLI_ORDER_ID
) t10 on t1.CLI_ORDER_ID = t10.CLI_ORDER_ID
LEFT JOIN (
SELECT 
    t.CLI_ORDER_ITEM_ID,
STUFF((
        SELECT ',' + t2.SURGERY_NAME
        FROM (
            SELECT DISTINCT isa.CLI_ORDER_ITEM_ID, isa.SURGERY_NAME
            FROM INP_SURGICAL_ANESTHESIA_PLAN isa
            WHERE isa.IS_DEL = 0 
                AND isa.SURGERY_NAME IS NOT NULL 
                AND isa.CLI_ORDER_ITEM_ID IS NOT NULL
  
```

## 目标表-概览

```sql
'--查询出目标值，各个指标编码是固定的
WITH TargetValue AS (
    SELECT 
        TARGET_COMP_VAL/100.0 AS target_value 
    FROM 
        MRAS_TARGET_DEFINITION (NOLOCK)
    WHERE 
        TARGET_NO = 'HXZD-011-001'
),
-- 按照科室来进行分组查询，用来查询哪个科室不达标
DeptOrderStats AS (
SELECT
event.DEPT_ID,
      event.DEPT_NAME AS "科室名称",
COUNT(CASE WHEN OPERATOR_OVERLAP = '98175' THEN 1 ELSE NULL END) AS '分子同一时间内手术医师为同一人的手术例数',
COUNT(1) AS '分母同期住院患者手术总例数',
CASE WHEN COUNT(1) = 0 THEN 0  ELSE COUNT(CASE WHEN OPERATOR_OVERLAP = '98175' THEN 1 ELSE NULL END) * 1.0 / COUNT(1) * 1.0 END AS '监测情况' 
FROM
MRAS_BUSINESS_SURGERY event (NOLOCK)
WHERE
--布局组件设置提升效率
#EQUALS{:onlySearchFeilds; ONLY_SEARCH_FEILDS;  1 = 0 ; 1=1}
AND event.SURGERY_END_AT BETWEEN :marptBeginAt and :marptEndAt
  #ETC{ AND event.HOSPITAL_AREA_ID IN (:hospitalAreaList) }
GROUP BY
        event.DEPT_ID, event.DEPT_NAME
),
-- 用来处理总数
TotalStats AS (
    SELECT
        SUM(分子同一时间内手术医师为同一人的手术例数) AS "分子同一时间内手术医师为同一人的手术例数",
        SUM(分母同期住院患者手术总例数) AS "分母同期住院患者手术总例数",
        CASE 
            WHEN SUM(分母同期住院患者手术总例数) = 0 THEN 0 
            ELSE SUM(分子同一时间内手术医师为同一人的手术例数) * 1.0 / SUM(分母同期住院患者手术总例数) 
        END AS "监测情况",
        (SELECT target_value FROM TargetValue) AS "目标值"
    FROM DeptOrderStats
)
-- 进行数据输出
SELECT
    t.*,
CASE WHEN t.监测情况 >= t.目标值 THEN '否' ELSE '是' END AS "是否达标",
    STUFF((
        SELECT ', ' + 科室名称 
        FROM DeptOrderStats 
        WHERE 分母同期住院患者手术总例数 > 0 
          AND 监测情况 >= (SELECT target_value FROM TargetValue)
        FOR XML PATH('')
    ), 1, 2, '') AS "未达标科室列表"
FROM TotalStats t;
```

## 目标表-科室统计

```sql
'--目标值的数据
WITH TargetValue AS (
    SELECT 
        TARGET_COMP_VAL/100.0 AS target_value 
    FROM 
        MRAS_TARGET_DEFINITION 
    WHERE 
        TARGET_NO = 'HXZD-011-001'
),
-- 按照科室来进行处理
DeptStats AS (
SELECT
event.BIZ_ATTR_ONE_ID AS "当前科室编码",
event.BIZ_ATTR_ONE_NAME AS "当前科室名称",
COUNT(CASE WHEN OPERATOR_OVERLAP = '98175' THEN 1 ELSE NULL END) AS "分子同一时间内手术医师为同一人的手术例数",
COUNT(1) AS "分母同期住院患者手术总例数",
CASE WHEN COUNT(1) = 0 THEN 0  ELSE COUNT(CASE WHEN OPERATOR_OVERLAP = '98175' THEN 1 ELSE NULL END) * 1.0 / COUNT(1) * 1.0 END AS "监测情况" ,
  max(event.HOSPITAL_AREA_ID) as HOSPITAL_AREA_ID
FROM
MRAS_BUSINESS_SURGERY event 
WHERE
--布局组件设置提升效率
#EQUALS{:onlySearchFeilds; ONLY_SEARCH_FEILDS; 1 = 0 ; 1= 1}
AND event.SURGERY_END_AT BETWEEN :marptBeginAt and :marptEndAt 
#ETC{AND event.BIZ_ATTR_ONE_ID IN (:deptIdIn)}
           #ETC{ AND event.HOSPITAL_AREA_ID IN (:hospitalAreaList) }
GROUP BY  event.BIZ_ATTR_ONE_ID,event.BIZ_ATTR_ONE_NAME
),
-- 条件进一步筛选
TempResults AS (
    SELECT
        d.当前科室编码,
        d.当前科室名称,
        d.分子同一时间内手术医师为同一人的手术例数,
        d.分母同期住院患者手术总例数,
        d.监测情况,
        (SELECT target_value FROM TargetValue) AS "目标值",
        CASE 
WHEN (SELECT target_value FROM TargetValue) IS NULL THEN NULL
            WHEN d.分母同期住院患者手术总例数 = 0 THEN '无数据'
            WHEN d.监测情况 < (SELECT target_value FROM TargetValue) THEN '达标'
            ELSE '未达标'
        END AS "对比结果",
  CASE 
WHEN (SELECT target_value FROM TargetValue) IS NULL THEN 98176
            WHEN d.分母同期住院患者手术总例数 = 0 THEN 98176
            WHEN d.监测情况 < (SELECT target_value FROM TargetValue) THEN 98175
            ELSE 98176
        END AS "standFlag",
  d.监测情况 as "resultVal",
        d.当前科室编码 as "deptId",
        d.当前科室名称 as "deptName",
        d.分子同一时间内手术医师为同一人的手术例数 as "numerator",
        d.分母同期住院患者手术总例数 as "denominator",
        d.HOSPITAL_AREA_ID as "hospitalAreaId"
    FROM 
        DeptStats d
WHERE 1 = 1 
-- 达标
#EQUALS{:qualified; 98175; AND d.监测情况 < (SELECT target_value FROM TargetValue) ; }
-- 未达标
#EQUALS{:qualified; 98176; AND d.监测情况 >= (SELECT target_value FROM TargetValue) ; }
)
-- 最终查询结果
SELECT * FROM TempResults
```

## 目标表-患者明细

```sql
'SELECT
event.ENCOUNTER_ID ,
event.DEPT_ID AS '当前科室编码',
event.DEPT_NAME AS '当前科室',
event.IMRN AS '住院号',
event.PERSON_NAME AS '患者姓名',
event.EMPLOYEE_NAME AS '责任医师',
team.ORG_NAME as "TEAM_NAME",
team.ORG_ID as "TEAM_ID",
team.ORG_NO as "TEAM_NO",
team.ORG_NAME as "当前医疗组",
event.ADMITTED_TO_WARD_AT AS '入区时间',
event.DISCHARGED_FROM_WARD_AT AS '出区时间',
event.SURGERY_NAME AS '手术名称',
event.SURGERY_START_AT AS '手术开始时间',
event.SURGERY_END_AT AS '手术结束时间',
CASE WHEN OPERATOR_OVERLAP = '98175' THEN '是' ELSE '否' END AS '主刀医师手术时间是否重合',
CASE WHEN OPERATOR_OVERLAP = '98175' THEN 98175 ELSE 98176 END AS "standFlag",
t11.SURGERY_NAME AS '重合手术名称',
t12.SURGERY_START_AT AS '重合手术开始时间',
t12.SURGERY_END_AT AS '重合手术结束时间' 
FROM
MRAS_BUSINESS_SURGERY event  (NOLOCK)
LEFT JOIN (
SELECT 
            t.CLI_ORDER_ITEM_ID,
            STUFF((
                SELECT ',' + t2.SURGERY_NAME
                FROM (
                    SELECT DISTINCT isa.CLI_ORDER_ITEM_ID, isa.SURGERY_NAME
                    FROM INP_SURGICAL_ANESTHESIA_PLAN isa WITH(NOLOCK)
                    WHERE isa.IS_DEL = 0 AND isa.SURGERY_NAME IS NOT NULL AND isa.CLI_ORDER_ITEM_ID IS NOT NULL 
                ) t2
                WHERE t2.CLI_ORDER_ITEM_ID = t.CLI_ORDER_ITEM_ID
                FOR XML PATH('')
            ), 1, 1, '') AS SURGERY_NAME,
            MAX(t.SURGICAL_ANESTHESIA_CODE) AS ANES_TYPE_ID
FROM (
SELECT DISTINCT isa.CLI_ORDER_ITEM_ID, isa.SURGERY_NAME, isa.SURGICAL_ANESTHESIA_CODE
FROM INP_SURGICAL_ANESTHESIA_PLAN isa
WHERE isa.IS_DEL = 0 AND isa.SURGERY_NAME IS NOT NULL AND isa.CLI_ORDER_ITEM_ID IS NOT NULL 
) t
GROUP BY t.CLI_ORDER_ITEM_ID
) t11 on event.OVERLAP_OPERATOR_SURGERIES = t11.CLI_ORDER_ITEM_ID
LEFT JOIN INP_SURGICAL_PLAN t12 on t12.CLI_ORDER_ITEM_ID = t11.CLI_ORDER_ITEM_ID
LEFT JOIN INPATIENT_ENCOUNTER inp (NOLOCK)  ON event.ENCOUNTER_ID = inp.ENCOUNTER_ID
LEFT JOIN MRAS_ORGANIZATION team (NOLOCK)  ON team.ORG_ID = inp.CURRENT_MEDICAL_GROUP_ID
WHERE
--布局组件设置提升效率
#EQUALS{:onlySearchFeilds; ONLY_SEARCH_FEILDS;  1 = 0 ; 1=1}
  AND event.SURGERY_END_AT BETWEEN :marptBeginAt and :marptEndAt
  #ETC{ AND event.HOSPITAL_AREA_ID IN (:hospitalAreaList) }
#ETC{AND event.DEPT_ID IN (:deptIdIn)}
#EQUALS{:status; 98175; AND OPERATOR_OVERLAP = '98175' ; }
#EQUALS{:status; 98176; AND OPERATOR_OVERLAP = '98176' ; }
```

## sql自定义参数

本模块用于定义 SQL 脚本中的动态占位符语法，支持条件分支与参数替换。以下为两种核心占位符的规范说明。

### #ETC — 条件占位符

**定义：** SQL 中的条件/其他占位符。入参存在时注入 SQL 片段，入参未传时自动移除该行。

**语法：** `#ETC{SQL片段}`

**示例：**

```sql
-- 原始脚本
WHERE 1=1
#ETC{AND ie.CURRENT_DEPT_ID NOT IN (:exDeptSet)}
#ETC{AND ie.FULL_NAME NOT LIKE :exPatientSet}
```

**展开规则：**

| 入参情况 | 最终 SQL |
|----------|----------|
| `exDeptSet=[101,102]`，`exPatientSet` 未传 | `WHERE 1=1`<br>`AND ie.CURRENT_DEPT_ID NOT IN (:exDeptSet)` |
| `exDeptSet` 未传，`exPatientSet='%张三%'` | `WHERE 1=1`<br>`AND ie.FULL_NAME NOT LIKE :exPatientSet` |
| 两者都未传 | `WHERE 1=1` |

---

### #EQUALS — 等值分支占位符

**定义：** SQL 中的等值比较占位符。根据入参值选择对应的 SQL 分支。

**语法：** `#EQUALS{:paramName; value; SQL片段}`

**示例：**

```sql
-- 原始脚本
WHERE 1=1
#EQUALS{:syncType; increment; AND (a.MODIFIED_AT BETWEEN :startTime AND :endTime)}
#EQUALS{:syncType; single; AND a.ENCOUNTER_ID IN (SELECT ... WHERE DISCHARGED_FROM_WARD_AT IS NULL OR ...)}
#EQUALS{:syncType; outHosp; AND a.ENCOUNTER_ID IN (SELECT ... WHERE DISCHARGED_FROM_WARD_AT BETWEEN ...)}
```

**展开规则：**

| 入参 | 值域 | 含义 | SQL 效果 |
|------|------|------|----------|
| `:syncType` | `increment` | 增量同步 | `AND (MODIFIED_AT BETWEEN :startTime AND :endTime)` |
| | `single` | 在院患者 | `AND ENCOUNTER_ID IN (SELECT ... WHERE DISCHARGED_FROM_WARD_AT IS NULL OR ...)` |
| | `outHosp` | 出院患者 | `AND ENCOUNTER_ID IN (SELECT ... WHERE DISCHARGED_FROM_WARD_AT BETWEEN ...)` |

---

### 使用说明

| 占位符类型 | 触发方式 | 多值行为 | 典型场景 |
|-----------|---------|---------|---------|
| `#ETC{}` | 入参存在则注入 | 支持多个独立 `#ETC{}` | 可选过滤条件、排除规则 |
| `#EQUALS{}` | 入参值匹配则注入 | 同一参数多个分支，互斥命中 | 同步模式切换、数据源选择 |

## 中间表

MRAS_BUSINESS_SURGERY

中间表 `MRAS_BUSINESS_SURGERY` 作为目标表，存储由源表经 XXJOB T+1 抽取后的业务指标数据。

## 业务表(不影响数据)

INPATIENT_ENCOUNTER, INP_SURGICAL_PLAN, MRAS_ORGANIZATION

以上业务表在 ETL 过程中仅作为关联查询的维度表，不产生新的业务数据。

## 业务表(影响数据)

INP_SURGICAL_ANESTHESIA_PLAN

以上业务表的数据变更会直接影响本指标的统计结果。

## 相关链接

- 指标概念：[[concepts/HXZD-011-001手术医师手术时间重合率]]
- ETL抽取逻辑：[[concepts/ETL抽取逻辑说明]]
- 证据链：[[references/evidence-chain]]
- 指标映射表：[[references/indicator-map]]
- 目标表-概览索引：[[concepts/目标表-概览索引]]
- 源表索引：[[concepts/源表索引]]

<!-- entity: HXZD-011-001 | code: HXZD-011-001 | system: 手术安全核查制度 | dim: 逻辑判定类 -->
