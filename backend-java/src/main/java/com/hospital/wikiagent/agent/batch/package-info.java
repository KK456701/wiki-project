/**
 * “计算全部指标结果”批量请求的确定性执行链路：意图检测、单指标执行、结果聚合与轻量持久化。
 *
 * <p>本包处理意图固定为试运行、指标身份可枚举、时间来自父请求的批量场景，全程 0 次 LLM
 * 调用；单个指标使用独立运行状态并由编排器做失败隔离，数据库并发由 ToolGateway 信号量兜底。</p>
 */
package com.hospital.wikiagent.agent.batch;
